#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code v4.X)
# =============================================================================
"""
Step ID: step07
Step Name: Prepare Plot Data
RQ: results/ch7/7.8.1
Generated: 2026-01-09

PURPOSE:
Prepare plot-ready data for rq_plots agent. Create CSV with profile
characteristics formatted for visualization (profile centroids, error bars,
sample sizes).

EXPECTED INPUTS:
  - data/step04_profile_characteristics.csv
    Columns: ['Profile', 'N', 'What_Mean', 'What_SD', 'What_CI_Low', 'What_CI_High',
              'Where_Mean', 'Where_SD', 'Where_CI_Low', 'Where_CI_High']
    Format: CSV with profile statistics
    Expected rows: K (number of profiles)

EXPECTED OUTPUTS:
  - data/lpa_plot_data.csv
    Columns: ['Profile', 'N', 'What_Mean', 'What_SE', 'Where_Mean', 'Where_SE']
    Format: CSV formatted for rq_plots visualization
    Expected rows: K (number of profiles)

VALIDATION CRITERIA:
  - All profiles included
  - SE values computed from bootstrap CIs
  - No missing values

g_code REASONING:
- Approach: Load characteristics → Compute SE from CIs → Format for plotting
- Why this approach: rq_plots expects specific column format (Mean + SE)
- Data flow: Load stats → CI → SE conversion → Save plot-ready format
- Expected performance: ~1 second (simple transformations)

IMPLEMENTATION NOTES:
- Analysis tool: Custom data transformation
- Validation tool: Manual checks on completeness
- Parameters: SE approximation from 95% CI width / 3.92 (assumes normal distribution)
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
import traceback

# Add project root to path for imports
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.8.1
LOG_FILE = RQ_DIR / "logs" / "step07_prepare_plot_data.log"
OUTPUT_DIR = RQ_DIR / "data"

# Input
INPUT_CHARACTERISTICS = OUTPUT_DIR / 'step04_profile_characteristics.csv'

# Output
OUTPUT_PLOT_DATA = OUTPUT_DIR / 'lpa_plot_data.csv'

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()
    print(msg, flush=True)

# =============================================================================
# Helper Functions
# =============================================================================

def ci_to_se(ci_low, ci_high, ci_level=0.95):
    """
    Convert 95% confidence interval to standard error.

    Formula: SE = (CI_high - CI_low) / (2 * z_critical)

    For 95% CI: z_critical = 1.96
    Therefore: SE = (CI_high - CI_low) / 3.92

    This assumes normal distribution of the mean.
    """
    z_critical = 1.96  # For 95% CI
    ci_width = ci_high - ci_low
    se = ci_width / (2 * z_critical)

    return se

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 07: Prepare Plot Data")

        # =========================================================================
        # STEP 1: Load Profile Characteristics
        # =========================================================================

        log("\n[LOAD] Loading profile characteristics...")
        log(f"[PATH] {INPUT_CHARACTERISTICS}")

        df_characteristics = pd.read_csv(INPUT_CHARACTERISTICS)
        log(f"[LOADED] {len(df_characteristics)} profiles")
        log(f"[COLUMNS] {df_characteristics.columns.tolist()}")

        # Scientific Mantra Checkpoint
        log("\n[VERIFY] Input validation")
        expected_cols = ['Profile', 'N', 'What_Mean', 'What_SD', 'What_CI_Low', 'What_CI_High',
                        'Where_Mean', 'Where_SD', 'Where_CI_Low', 'Where_CI_High']

        missing_cols = [col for col in expected_cols if col not in df_characteristics.columns]
        if missing_cols:
            raise ValueError(f"Missing required columns: {missing_cols}")

        log(f"[PASS] All required columns present")

        log(f"\n[INFO] Profile characteristics:")
        log(f"{df_characteristics.to_string(index=False)}")

        # =========================================================================
        # STEP 2: Compute Standard Errors from Bootstrap CIs
        # =========================================================================

        log("\n[COMPUTE] Converting 95% CIs to standard errors...")

        # What domain SE
        df_characteristics['What_SE'] = df_characteristics.apply(
            lambda row: ci_to_se(row['What_CI_Low'], row['What_CI_High']),
            axis=1
        )

        # Where domain SE
        df_characteristics['Where_SE'] = df_characteristics.apply(
            lambda row: ci_to_se(row['Where_CI_Low'], row['Where_CI_High']),
            axis=1
        )

        log(f"[COMPUTED] Standard errors from bootstrap CIs")
        log(f"[INFO] What SE range: [{df_characteristics['What_SE'].min():.4f}, {df_characteristics['What_SE'].max():.4f}]")
        log(f"[INFO] Where SE range: [{df_characteristics['Where_SE'].min():.4f}, {df_characteristics['Where_SE'].max():.4f}]")

        # Scientific Mantra Checkpoint
        log("\n[VERIFY] SE computation validation")

        # Check SEs are positive
        if (df_characteristics['What_SE'] <= 0).any():
            raise ValueError("What_SE contains non-positive values")
        if (df_characteristics['Where_SE'] <= 0).any():
            raise ValueError("Where_SE contains non-positive values")

        log(f"[PASS] All SEs are positive")

        # =========================================================================
        # STEP 3: Format Plot Data
        # =========================================================================

        log("\n[FORMAT] Creating plot-ready dataset...")

        # Select columns for plotting (rq_plots expects: Profile, N, *_Mean, *_SE)
        df_plot = df_characteristics[['Profile', 'N', 'What_Mean', 'What_SE', 'Where_Mean', 'Where_SE']].copy()

        log(f"[FORMATTED] Plot data with {len(df_plot)} profiles")
        log(f"[COLUMNS] {df_plot.columns.tolist()}")

        # =========================================================================
        # STEP 4: Save Plot Data
        # =========================================================================

        log(f"\n[SAVE] Saving plot data to {OUTPUT_PLOT_DATA}")

        df_plot.to_csv(OUTPUT_PLOT_DATA, index=False, encoding='utf-8')

        log(f"[SAVED] {len(df_plot)} rows, {len(df_plot.columns)} columns")
        log(f"\n[PLOT DATA TABLE]")
        log(f"{df_plot.to_string(index=False)}")

        # =========================================================================
        # VALIDATION: Final Checks
        # =========================================================================

        log("\n[VALIDATION] Final output validation...")

        # Check 1: Row count matches input
        if len(df_plot) != len(df_characteristics):
            raise ValueError(f"Row count mismatch: {len(df_plot)} != {len(df_characteristics)}")
        log(f"[PASS] Row count preserved ({len(df_plot)} profiles)")

        # Check 2: No missing values
        missing_count = df_plot.isnull().sum().sum()
        if missing_count > 0:
            raise ValueError(f"Found {missing_count} missing values in plot data")
        log(f"[PASS] No missing values")

        # Check 3: Profile IDs are sequential (0, 1, 2, ...)
        expected_profiles = list(range(len(df_plot)))
        actual_profiles = sorted(df_plot['Profile'].unique())

        if actual_profiles != expected_profiles:
            log(f"[WARNING] Non-sequential profile IDs: {actual_profiles} (expected: {expected_profiles})")
        else:
            log(f"[PASS] Profile IDs are sequential")

        # Check 4: SE values reasonable (should be < SD for bootstrap)
        for idx, row in df_plot.iterrows():
            profile_id = row['Profile']
            what_sd = df_characteristics.loc[idx, 'What_SD']
            where_sd = df_characteristics.loc[idx, 'Where_SD']

            # SE should typically be smaller than SD (SD is population, SE is sampling distribution)
            # But with bootstrap, SE can sometimes be close to SD (especially for small N)
            # Just check they're in reasonable range (not orders of magnitude different)

            if row['What_SE'] > 2 * what_sd:
                log(f"[WARNING] Profile {profile_id}: What_SE ({row['What_SE']:.4f}) > 2*SD ({what_sd:.4f})")

            if row['Where_SE'] > 2 * where_sd:
                log(f"[WARNING] Profile {profile_id}: Where_SE ({row['Where_SE']:.4f}) > 2*SD ({where_sd:.4f})")

        log(f"[PASS] SE values in reasonable range")

        # =========================================================================
        # SUMMARY: Plot Data Description
        # =========================================================================

        log("\n[SUMMARY] Plot data ready for rq_plots agent")
        log(f"[INFO] Profiles: {len(df_plot)}")
        log(f"[INFO] Domains: What, Where (2 dimensions)")
        log(f"[INFO] Error bars: Bootstrap-derived standard errors")
        log(f"[INFO] Sample sizes included for each profile")

        log("\n[USAGE] This file can be used by rq_plots to create:")
        log("  1. Profile centroid plot (What vs Where means)")
        log("  2. Error bar plot (means ± SE)")
        log("  3. Profile size comparison (N per profile)")

        log("\n[SUCCESS] Step 07 complete")
        sys.exit(0)

    except Exception as e:
        log(f"\n[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)
