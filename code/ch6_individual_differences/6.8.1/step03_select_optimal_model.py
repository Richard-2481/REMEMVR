#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code v4.X)
# =============================================================================
"""
Step ID: step03
Step Name: Select Optimal Model
RQ: results/ch7/7.8.1
Generated: 2026-01-09

PURPOSE:
Select optimal K solution based on BIC minimum, profile size constraints (N>=20),
and entropy threshold (>0.80). Extract profile assignments and posterior
probabilities for the selected optimal model.

EXPECTED INPUTS:
  - data/step02_lpa_fit_comparison.csv
    Columns: ['K', 'BIC', 'AIC', 'Entropy', 'Converged']
    Format: CSV with fit indices for K=1-4
    Expected rows: 4

  - data/step02_lpa_fitted_models.pkl
    Format: Pickle with fitted GaussianMixture models
    Expected content: {1: model_K1, 2: model_K2, 3: model_K3, 4: model_K4}

  - data/step01_domain_theta_scores.csv
    Columns: ['UID', 'theta_What', 'theta_Where']
    Format: CSV with participant-level theta scores
    Expected rows: 100

EXPECTED OUTPUTS:
  - data/step03_optimal_profiles.csv
    Columns: ['UID', 'Profile', 'Max_Posterior_Prob']
    Format: CSV with profile assignments for optimal K
    Expected rows: 100

  - data/step03_model_selection_summary.txt
    Format: Text file documenting model selection process
    Expected content: Selection criteria, optimal K, profile sizes

VALIDATION CRITERIA:
  - Optimal K identified (BIC minimum)
  - All profiles have N>=20 participants
  - Entropy >0.80 for good classification
  - All participants assigned to profiles

g_code REASONING:
- Approach: Hierarchical selection (BIC → profile size → entropy)
- Why this approach: Balances statistical fit with practical interpretability
- Data flow: Load fit indices → Apply criteria → Select K → Extract assignments
- Expected performance: ~1 second (model selection logic)

IMPLEMENTATION NOTES:
- Analysis tool: Custom selection logic (BIC-based with constraints)
- Validation tool: Manual checks on profile sizes and entropy
- Parameters: min_profile_size=20, entropy_threshold=0.80
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
import pickle
import traceback

# Add project root to path for imports
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.8.1
LOG_FILE = RQ_DIR / "logs" / "step03_select_optimal_model.log"
OUTPUT_DIR = RQ_DIR / "data"

# Inputs
INPUT_FIT = OUTPUT_DIR / 'step02_lpa_fit_comparison.csv'
INPUT_MODELS = OUTPUT_DIR / 'step02_lpa_fitted_models.pkl'
INPUT_THETA = OUTPUT_DIR / 'step01_domain_theta_scores.csv'

# Outputs
OUTPUT_PROFILES = OUTPUT_DIR / 'step03_optimal_profiles.csv'
OUTPUT_SUMMARY = OUTPUT_DIR / 'step03_model_selection_summary.txt'

# Selection Criteria
MIN_PROFILE_SIZE = 20
ENTROPY_THRESHOLD = 0.80

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()
    print(msg, flush=True)

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 03: Select Optimal Model")

        # =========================================================================
        # STEP 1: Load Fit Comparison Results
        # =========================================================================
        # Expected: K=1-4 with BIC, AIC, Entropy, Converged
        # Purpose: Model selection criteria

        log("\n[LOAD] Loading fit comparison results...")
        log(f"[PATH] {INPUT_FIT}")

        df_fit = pd.read_csv(INPUT_FIT)
        log(f"[LOADED] {len(df_fit)} models")
        log(f"\n[FIT TABLE]")
        log(f"{df_fit.to_string(index=False)}")

        # Scientific Mantra Checkpoint
        log("\n[VERIFY] Fit comparison validation")
        expected_cols = ['K', 'BIC', 'AIC', 'Entropy', 'Converged']
        if df_fit.columns.tolist() != expected_cols:
            raise ValueError(f"Column mismatch in fit comparison")
        log("[PASS] Fit comparison structure validated")

        # =========================================================================
        # STEP 2: Load Fitted Models
        # =========================================================================
        # Purpose: Extract profile assignments from optimal model

        log(f"\n[LOAD] Loading fitted models...")
        log(f"[PATH] {INPUT_MODELS}")

        with open(INPUT_MODELS, 'rb') as f:
            fitted_models = pickle.load(f)

        log(f"[LOADED] {len(fitted_models)} models (K={list(fitted_models.keys())})")

        # =========================================================================
        # STEP 3: Load Theta Scores
        # =========================================================================
        # Purpose: Participant data for profile assignment

        log(f"\n[LOAD] Loading theta scores...")
        log(f"[PATH] {INPUT_THETA}")

        df_theta = pd.read_csv(INPUT_THETA)
        log(f"[LOADED] {len(df_theta)} participants")

        X = df_theta[['theta_What', 'theta_Where']].values
        log(f"[INFO] Feature matrix shape: {X.shape}")

        # =========================================================================
        # STEP 4: Select Optimal K
        # =========================================================================
        # Selection hierarchy:
        # 1. BIC minimum (primary criterion)
        # 2. Profile size constraint (all profiles N>=20)
        # 3. Entropy threshold (>0.80 for good classification)

        log("\n[SELECT] Applying model selection criteria...")
        log(f"[CRITERIA] 1. BIC minimum (primary)")
        log(f"[CRITERIA] 2. All profiles N>={MIN_PROFILE_SIZE}")
        log(f"[CRITERIA] 3. Entropy >{ENTROPY_THRESHOLD}")

        # Identify BIC minimum
        bic_min_idx = df_fit['BIC'].idxmin()
        optimal_k = df_fit.loc[bic_min_idx, 'K']
        bic_min_value = df_fit.loc[bic_min_idx, 'BIC']

        log(f"\n[BIC] Minimum BIC at K={optimal_k} (BIC={bic_min_value:.2f})")

        # Check constraints for optimal K
        optimal_model = fitted_models[optimal_k]
        optimal_entropy = df_fit.loc[bic_min_idx, 'Entropy']
        optimal_converged = df_fit.loc[bic_min_idx, 'Converged']

        log(f"[INFO] Optimal K={optimal_k} properties:")
        log(f"  - Entropy: {optimal_entropy:.4f}")
        log(f"  - Converged: {optimal_converged}")

        # Get profile assignments for optimal K
        profile_labels = optimal_model.predict(X)
        profile_counts = pd.Series(profile_labels).value_counts().sort_index()

        log(f"[INFO] Profile sizes for K={optimal_k}:")
        for profile, count in profile_counts.items():
            log(f"  - Profile {profile}: N={count}")

        # Check profile size constraint
        min_profile_count = profile_counts.min()
        if min_profile_count < MIN_PROFILE_SIZE:
            log(f"[WARNING] Smallest profile (N={min_profile_count}) below threshold (N>={MIN_PROFILE_SIZE})")
        else:
            log(f"[PASS] All profiles meet size constraint (min N={min_profile_count})")

        # Check entropy constraint
        if optimal_entropy < ENTROPY_THRESHOLD:
            log(f"[WARNING] Entropy ({optimal_entropy:.4f}) below threshold ({ENTROPY_THRESHOLD})")
        else:
            log(f"[PASS] Entropy meets threshold ({optimal_entropy:.4f} > {ENTROPY_THRESHOLD})")

        # Scientific Mantra Checkpoint
        log(f"\n[DECISION] Optimal K selected: K={optimal_k}")

        # =========================================================================
        # STEP 5: Extract Profile Assignments
        # =========================================================================
        # Purpose: Assign participants to profiles with posterior probabilities

        log(f"\n[EXTRACT] Extracting profile assignments for K={optimal_k}...")

        # Get posterior probabilities
        posterior_probs = optimal_model.predict_proba(X)

        # Create output dataframe
        df_profiles = df_theta[['UID']].copy()
        df_profiles['Profile'] = profile_labels
        df_profiles['Max_Posterior_Prob'] = posterior_probs.max(axis=1)

        log(f"[EXTRACTED] {len(df_profiles)} profile assignments")
        log(f"[INFO] Posterior probability summary:")
        log(f"{df_profiles['Max_Posterior_Prob'].describe().to_string()}")

        # Count high-confidence assignments (>80%)
        high_confidence = (df_profiles['Max_Posterior_Prob'] > 0.80).sum()
        pct_high_confidence = 100.0 * high_confidence / len(df_profiles)

        log(f"[INFO] High confidence assignments (>80%): {high_confidence}/{len(df_profiles)} ({pct_high_confidence:.1f}%)")

        # =========================================================================
        # STEP 6: Save Profile Assignments
        # =========================================================================

        log(f"\n[SAVE] Saving profile assignments to {OUTPUT_PROFILES}")

        df_profiles.to_csv(OUTPUT_PROFILES, index=False, encoding='utf-8')

        log(f"[SAVED] {len(df_profiles)} rows")
        log(f"[COLUMNS] {df_profiles.columns.tolist()}")

        # =========================================================================
        # STEP 7: Save Model Selection Summary
        # =========================================================================

        log(f"\n[SAVE] Saving model selection summary to {OUTPUT_SUMMARY}")

        with open(OUTPUT_SUMMARY, 'w', encoding='utf-8') as f:
            f.write("=" * 80 + "\n")
            f.write("MODEL SELECTION SUMMARY - RQ 7.8.1 LPA\n")
            f.write("=" * 80 + "\n\n")

            f.write("SELECTION CRITERIA (Hierarchical):\n")
            f.write(f"  1. BIC minimum (primary criterion)\n")
            f.write(f"  2. All profiles N >= {MIN_PROFILE_SIZE} participants\n")
            f.write(f"  3. Entropy > {ENTROPY_THRESHOLD} (good classification)\n\n")

            f.write("-" * 80 + "\n")
            f.write("FIT COMPARISON TABLE\n")
            f.write("-" * 80 + "\n\n")
            f.write(df_fit.to_string(index=False))
            f.write("\n\n")

            f.write("-" * 80 + "\n")
            f.write("OPTIMAL MODEL SELECTION\n")
            f.write("-" * 80 + "\n\n")
            f.write(f"Optimal K: {optimal_k}\n")
            f.write(f"BIC: {bic_min_value:.2f}\n")
            f.write(f"Entropy: {optimal_entropy:.4f}\n")
            f.write(f"Converged: {optimal_converged}\n\n")

            f.write("Profile Sizes:\n")
            for profile, count in profile_counts.items():
                f.write(f"  Profile {profile}: N={count}\n")

            f.write(f"\nMinimum Profile Size: {min_profile_count}\n")
            f.write(f"Profile Size Constraint Met: {min_profile_count >= MIN_PROFILE_SIZE}\n")
            f.write(f"Entropy Threshold Met: {optimal_entropy >= ENTROPY_THRESHOLD}\n\n")

            f.write("-" * 80 + "\n")
            f.write("CLASSIFICATION QUALITY\n")
            f.write("-" * 80 + "\n\n")
            f.write(f"Mean Posterior Probability: {df_profiles['Max_Posterior_Prob'].mean():.4f}\n")
            f.write(f"Median Posterior Probability: {df_profiles['Max_Posterior_Prob'].median():.4f}\n")
            f.write(f"Min Posterior Probability: {df_profiles['Max_Posterior_Prob'].min():.4f}\n")
            f.write(f"High Confidence (>80%): {high_confidence}/{len(df_profiles)} ({pct_high_confidence:.1f}%)\n\n")

        log(f"[SAVED] Model selection summary written")

        # =========================================================================
        # VALIDATION: Check Output Quality
        # =========================================================================

        log("\n[VALIDATION] Output validation...")

        # Check 1: All participants assigned
        if len(df_profiles) != 100:
            raise ValueError(f"Expected 100 participants, got {len(df_profiles)}")
        log(f"[PASS] All 100 participants assigned to profiles")

        # Check 2: No missing values
        missing_count = df_profiles.isnull().sum().sum()
        if missing_count > 0:
            raise ValueError(f"Found {missing_count} missing values in profile assignments")
        log(f"[PASS] No missing values")

        # Check 3: Profile labels in expected range
        if df_profiles['Profile'].min() < 0 or df_profiles['Profile'].max() >= optimal_k:
            raise ValueError(f"Profile labels out of range [0, {optimal_k-1}]")
        log(f"[PASS] Profile labels in valid range")

        log("\n[SUCCESS] Step 03 complete")
        log(f"[RESULT] Optimal solution: K={optimal_k} profiles")
        sys.exit(0)

    except Exception as e:
        log(f"\n[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)
