#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code v4.X)
# =============================================================================
"""
Step ID: step00
Step Name: Validate Dependencies
RQ: results/ch7/7.8.1
Generated: 2026-01-09

PURPOSE:
Verify Ch5 domain-specific theta scores (from 5.2.1 ONLY) and cognitive test
data exist before proceeding with LPA analysis.

CRITICAL CORRECTION:
- Original 4_analysis.yaml incorrectly referenced THREE RQs (5.2.1, 5.2.2, 5.2.3)
- ACTUAL source: Ch5 5.2.1 ONLY (contains ALL three domains in ONE file)
- File: results/ch5/5.2.1/data/step03_theta_scores.csv
- Columns: composite_ID, theta_what, theta_where, theta_when

EXPECTED INPUTS:
  - results/ch5/5.2.1/data/step03_theta_scores.csv
    Columns: ['composite_ID', 'theta_what', 'theta_where', 'theta_when']
    Format: CSV with 400 rows (100 UIDs × 4 tests)
    Expected rows: 400

  - data/dfnonvr.csv
    Columns: ['UID', 'age', 'nart-score', 'rpm-score', 'ravlt-trial-*-score', 'bvmt-trial-*-score']
    Format: CSV with participant-level cognitive test data
    Expected rows: 100+

EXPECTED OUTPUTS:
  - data/step00_dependency_validation.txt
    Format: Text file with validation results for all dependencies
    Expected content: Validation status (PASS/FAIL) for each file

VALIDATION CRITERIA:
  - All files exist and readable
  - Files have minimum size (>1000 bytes)
  - CSV files have expected columns

g_code REASONING:
- Approach: File existence + column validation before expensive LPA analysis
- Why this approach: Prevents wasting computation on missing dependencies
- Data flow: Validates inputs → Reports PASS/FAIL status → Exits
- Expected performance: ~1 second (fast file checks)

IMPLEMENTATION NOTES:
- Analysis tool: Custom validation (no catalogued tool needed)
- Validation tool: Manual file checks with pd.read_csv
- Parameters: File paths, expected columns, minimum size thresholds
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import traceback

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/ch7/7.8.1/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = 7.8.1/
#   parents[2] = ch7/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.8.1
LOG_FILE = RQ_DIR / "logs" / "step00_validate_dependencies.log"
OUTPUT_FILE = RQ_DIR / "data" / "step00_dependency_validation.txt"

# Files to validate
DEPENDENCIES = [
    {
        'path': PROJECT_ROOT / 'results' / 'ch5' / '5.2.1' / 'data' / 'step03_theta_scores.csv',
        'description': 'Ch5 5.2.1 domain theta scores (What/Where/When)',
        'required_columns': ['composite_ID', 'theta_what', 'theta_where', 'theta_when'],
        'min_size_bytes': 1000,
        'expected_rows': 400
    },
    {
        'path': PROJECT_ROOT / 'data' / 'dfnonvr.csv',
        'description': 'Participant cognitive test data',
        'required_columns': ['UID', 'age', 'nart-score', 'rpm-score'],
        'min_size_bytes': 1000,
        'expected_rows': 100
    }
]

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()
    print(msg, flush=True)

# =============================================================================
# Main Validation
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 00: Validate Dependencies")

        # =========================================================================
        # VALIDATION: Check All Dependency Files
        # =========================================================================
        # Expected: All files exist with correct columns
        # Purpose: Prevent downstream failures due to missing inputs

        validation_results = []
        all_valid = True

        for dep in DEPENDENCIES:
            log(f"\n[CHECK] Validating: {dep['description']}")
            log(f"[PATH] {dep['path']}")

            result = {
                'file': dep['description'],
                'path': str(dep['path']),
                'exists': False,
                'size_ok': False,
                'columns_ok': False,
                'rows_ok': False,
                'status': 'FAIL'
            }

            # Check 1: File exists
            if not dep['path'].exists():
                log(f"[FAIL] File does not exist: {dep['path']}")
                all_valid = False
                validation_results.append(result)
                continue

            result['exists'] = True
            log(f"[PASS] File exists")

            # Check 2: File size
            file_size = dep['path'].stat().st_size
            if file_size < dep['min_size_bytes']:
                log(f"[FAIL] File too small: {file_size} bytes (min: {dep['min_size_bytes']})")
                all_valid = False
                validation_results.append(result)
                continue

            result['size_ok'] = True
            log(f"[PASS] File size OK: {file_size} bytes")

            # Check 3: CSV columns (header only for speed)
            try:
                df_header = pd.read_csv(dep['path'], nrows=0)
                actual_columns = df_header.columns.tolist()

                # Check required columns are present (not exact match - allows extra columns)
                missing_cols = [col for col in dep['required_columns'] if col not in actual_columns]

                if missing_cols:
                    log(f"[FAIL] Missing columns: {missing_cols}")
                    log(f"[INFO] Expected: {dep['required_columns']}")
                    log(f"[INFO] Found: {actual_columns}")
                    all_valid = False
                    validation_results.append(result)
                    continue

                result['columns_ok'] = True
                log(f"[PASS] Required columns present: {dep['required_columns']}")

                # Check 4: Row count (load full file)
                df_full = pd.read_csv(dep['path'])
                actual_rows = len(df_full)

                # Allow some flexibility in row count (at least 90% of expected)
                min_expected_rows = int(dep['expected_rows'] * 0.9)

                if actual_rows < min_expected_rows:
                    log(f"[FAIL] Too few rows: {actual_rows} (expected ~{dep['expected_rows']})")
                    all_valid = False
                    validation_results.append(result)
                    continue

                result['rows_ok'] = True
                log(f"[PASS] Row count OK: {actual_rows} rows (expected ~{dep['expected_rows']})")

                # All checks passed
                result['status'] = 'PASS'
                log(f"[SUCCESS] {dep['description']} validated successfully")

            except Exception as e:
                log(f"[FAIL] Error reading CSV: {str(e)}")
                all_valid = False
                validation_results.append(result)
                continue

            validation_results.append(result)

        # =========================================================================
        # SAVE VALIDATION RESULTS
        # =========================================================================
        # Purpose: Document validation status for downstream steps

        log(f"\n[SAVE] Writing validation results to {OUTPUT_FILE}")

        with open(OUTPUT_FILE, 'w', encoding='utf-8') as f:
            f.write("=" * 80 + "\n")
            f.write("DEPENDENCY VALIDATION REPORT - RQ 7.8.1\n")
            f.write("=" * 80 + "\n\n")

            f.write("CRITICAL CORRECTION:\n")
            f.write("- Original 4_analysis.yaml referenced THREE RQs (5.2.1, 5.2.2, 5.2.3)\n")
            f.write("- ACTUAL source: Ch5 5.2.1 ONLY (all domains in one file)\n")
            f.write("- Validation adjusted accordingly\n\n")

            for result in validation_results:
                f.write("-" * 80 + "\n")
                f.write(f"File: {result['file']}\n")
                f.write(f"Path: {result['path']}\n")
                f.write(f"Exists: {'YES' if result['exists'] else 'NO'}\n")
                f.write(f"Size OK: {'YES' if result['size_ok'] else 'NO'}\n")
                f.write(f"Columns OK: {'YES' if result['columns_ok'] else 'NO'}\n")
                f.write(f"Rows OK: {'YES' if result['rows_ok'] else 'NO'}\n")
                f.write(f"Status: {result['status']}\n\n")

            f.write("=" * 80 + "\n")
            f.write(f"OVERALL STATUS: {'PASS - All dependencies valid' if all_valid else 'FAIL - See errors above'}\n")
            f.write("=" * 80 + "\n")

        log(f"[SAVED] Validation report written")

        # =========================================================================
        # FINAL STATUS
        # =========================================================================

        if all_valid:
            log("\n[SUCCESS] Step 00 complete - All dependencies validated")
            sys.exit(0)
        else:
            log("\n[ERROR] Step 00 failed - Dependency validation errors detected")
            log("[ACTION] Fix missing/invalid dependencies before proceeding")
            sys.exit(1)

    except Exception as e:
        log(f"\n[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)
