#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step01
Step Name: extract_domain_theta_scores
RQ: results/ch7/7.4.2
Generated: 2026-01-06T16:20:00Z

PURPOSE:
Extract Where and What domain-specific theta scores from Ch5 analyses.
Load Ch5 theta scores, extract UID from composite_ID, aggregate across tests,
and create domain-specific mean scores for bivariate correlation analysis.

EXPECTED INPUTS:
  - results/ch5/5.2.1/data/step03_theta_scores.csv
    Columns: ['composite_ID', 'theta_what', 'theta_where', 'theta_when']
    Format: IRT theta scores with composite_ID (e.g., A010_1, A010_2)
    Expected rows: ~400 (100 participants × 4 tests)

EXPECTED OUTPUTS:
  - results/ch7/7.4.2/data/step01_domain_theta_scores.csv
    Columns: ['UID', 'Where_mean', 'What_mean']  
    Format: Participant-level aggregated domain theta scores
    Expected rows: ~100 (1 row per participant)

VALIDATION CRITERIA:
  - 100 participants with domain theta scores
  - Theta scores in IRT range [-3, 3]
  - Both Where and What domains have adequate variance
  - No missing UIDs or theta values

g_code REASONING:
- Approach: Extract UID from composite_ID, group by participant, compute domain means
- Why this approach: Ch5 saves test-level theta scores, but Ch7 needs participant-level scores
- Data flow: Ch5 test-level (400 rows) -> participant-level aggregation (100 rows) -> domain extraction
- Expected performance: ~seconds (simple pandas operations)

IMPLEMENTATION NOTES:
- Analysis tool: Custom implementation (tools.data.extract_domain_theta_scores signature mismatch)
- Validation tool: Custom validation (validate_numeric_range signature mismatch) 
- Parameters: Focus on Where and What domains only (exclude When/temporal)
- Key transformation: composite_ID "A010_1" -> UID "A010", then group and aggregate
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = 7.4.2/
#   parents[2] = ch7/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.4.2 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step01_extract_domain_theta_scores.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step01_domain_theta_scores.csv
#   CORRECT: data/step03_analysis_dataset.csv
#   WRONG:   results/domain_theta_scores.csv    (wrong folder + no prefix)
#   WRONG:   data/theta_scores.csv              (missing step prefix)
#   WRONG:   logs/step01_domain_scores.csv      (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()  # Critical for real-time monitoring
    print(msg, flush=True)  # -u flag compatibility

# =============================================================================
# Custom Analysis Functions (Due to Signature Mismatches)
# =============================================================================

def extract_domain_theta_scores_custom(input_path: str, domains: List[str]) -> pd.DataFrame:
    """
    Custom function to extract and aggregate domain theta scores.
    
    Parameters
    ----------
    input_path : str
        Path to Ch5 theta scores CSV
    domains : List[str]
        List of domains to extract (e.g., ['Where', 'What'])
    
    Returns
    -------
    pd.DataFrame
        Aggregated domain scores with columns: UID, {domain}_mean, ...
    """
    # Load Ch5 theta scores
    df_theta = pd.read_csv(input_path)
    log(f"[LOADED] Ch5 theta scores: {len(df_theta)} rows, columns: {df_theta.columns.tolist()}")
    
    # Extract UID from composite_ID (e.g., A010_1 -> A010)
    df_theta['UID'] = df_theta['composite_ID'].str.split('_').str[0]
    log(f"[TRANSFORM] Extracted UID from composite_ID")
    
    # Check unique participants
    n_participants = df_theta['UID'].nunique()
    log(f"[CHECK] Found {n_participants} unique participants")
    
    # Prepare domain columns mapping (handle case sensitivity)
    domain_cols = {}
    for domain in domains:
        # Map domain names to actual column names (lowercase theta_)
        col_name = f"theta_{domain.lower()}"
        if col_name in df_theta.columns:
            domain_cols[domain] = col_name
        else:
            log(f"[ERROR] Domain column not found: {col_name}")
            raise ValueError(f"Column {col_name} not found in theta scores")
    
    log(f"[MAPPING] Domain column mapping: {domain_cols}")
    
    # Group by UID and compute mean theta scores across tests
    agg_data = []
    for uid, group in df_theta.groupby('UID'):
        row_data = {'UID': uid}
        
        for domain, col_name in domain_cols.items():
            # Compute mean across all tests for this participant and domain
            domain_mean = group[col_name].mean()
            row_data[f"{domain}_mean"] = domain_mean
        
        agg_data.append(row_data)
    
    df_aggregated = pd.DataFrame(agg_data)
    log(f"[AGGREGATE] Created aggregated dataset: {len(df_aggregated)} participants")
    
    return df_aggregated

def validate_domain_theta_scores(df: pd.DataFrame, theta_range: Tuple[float, float] = (-3.0, 3.0)) -> Dict[str, Any]:
    """
    Custom validation for domain theta scores.
    
    Parameters
    ----------
    df : pd.DataFrame
        Domain theta scores to validate
    theta_range : Tuple[float, float]
        Valid IRT theta range (min, max)
        
    Returns
    -------
    Dict[str, Any]
        Validation results
    """
    results = {
        'valid': True,
        'n_participants': len(df),
        'missing_values': {},
        'out_of_range': {},
        'variance_check': {},
        'issues': []
    }
    
    min_theta, max_theta = theta_range
    
    # Check participant count
    if len(df) < 100:
        results['valid'] = False
        results['issues'].append(f"Insufficient participants: {len(df)} < 100")
    
    # Check for missing values
    for col in df.columns:
        if col != 'UID':
            missing_count = df[col].isna().sum()
            results['missing_values'][col] = missing_count
            if missing_count > 0:
                results['valid'] = False
                results['issues'].append(f"Missing values in {col}: {missing_count}")
    
    # Check theta ranges
    for col in df.columns:
        if col != 'UID' and 'mean' in col:
            out_of_range = ((df[col] < min_theta) | (df[col] > max_theta)).sum()
            results['out_of_range'][col] = out_of_range
            if out_of_range > 0:
                results['valid'] = False
                results['issues'].append(f"Out of range values in {col}: {out_of_range}")
            
            # Check variance
            variance = df[col].var()
            results['variance_check'][col] = variance
            if variance < 0.1:  # Very low variance threshold
                results['issues'].append(f"Low variance in {col}: {variance:.4f}")
    
    # Check for duplicate UIDs
    duplicates = df['UID'].duplicated().sum()
    if duplicates > 0:
        results['valid'] = False
        results['issues'].append(f"Duplicate UIDs found: {duplicates}")
    
    return results

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 01: extract_domain_theta_scores")

        # =========================================================================
        # STEP 1: Load Ch5 Domain Theta Scores
        # =========================================================================
        # Expected: Ch5 IRT theta scores with composite_ID (test-level data)
        # Purpose: Extract domain-specific theta scores for Where and What domains
        
        log("[LOAD] Loading Ch5 theta scores...")
        ch5_theta_path = PROJECT_ROOT / "results" / "ch5" / "5.2.1" / "data" / "step03_theta_scores.csv"
        
        if not ch5_theta_path.exists():
            raise FileNotFoundError(f"Ch5 theta scores not found: {ch5_theta_path}")
        
        log(f"[FOUND] Ch5 theta file: {ch5_theta_path}")

        # =========================================================================
        # STEP 2: Extract and Aggregate Domain Scores
        # =========================================================================
        # Tool: Custom implementation (extract_domain_theta_scores signature mismatch)
        # What it does: Extract UID, group by participant, compute domain means
        # Expected output: 100 participants with Where_mean and What_mean scores
        
        log("[ANALYSIS] Extracting and aggregating domain theta scores...")
        domains = ["Where", "What"]  # Focus on spatial and object domains
        
        df_domain_scores = extract_domain_theta_scores_custom(
            input_path=str(ch5_theta_path),
            domains=domains
        )
        log("[DONE] Domain score extraction complete")

        # =========================================================================
        # STEP 3: Save Domain Theta Scores
        # =========================================================================
        # These outputs will be used by: Step 03 (merge with BVMT scores)
        
        output_path = RQ_DIR / "data" / "step01_domain_theta_scores.csv"
        log(f"[SAVE] Saving domain theta scores to {output_path}...")
        
        # Ensure data directory exists
        output_path.parent.mkdir(parents=True, exist_ok=True)
        
        # Save with UTF-8 encoding
        df_domain_scores.to_csv(output_path, index=False, encoding='utf-8')
        log(f"[SAVED] {output_path} ({len(df_domain_scores)} rows, {len(df_domain_scores.columns)} cols)")
        
        # Log sample of data for verification
        log("[SAMPLE] First 3 rows:")
        for i, row in df_domain_scores.head(3).iterrows():
            log(f"  {dict(row)}")

        # =========================================================================
        # STEP 4: Run Validation
        # =========================================================================
        # Tool: Custom validation (validate_numeric_range signature mismatch)
        # Validates: Participant count, theta ranges, missing values, variance
        # Threshold: 100 participants, theta in [-3, 3], adequate variance
        
        log("[VALIDATION] Validating domain theta scores...")
        validation_result = validate_domain_theta_scores(
            df=df_domain_scores,
            theta_range=(-3.0, 3.0)  # Standard IRT theta range
        )

        # Report validation results
        # Expected: All validation checks pass for downstream analysis
        log(f"[VALIDATION] Overall valid: {validation_result['valid']}")
        log(f"[VALIDATION] Participants: {validation_result['n_participants']}")
        log(f"[VALIDATION] Missing values: {validation_result['missing_values']}")
        log(f"[VALIDATION] Out of range: {validation_result['out_of_range']}")
        log(f"[VALIDATION] Variance check: {validation_result['variance_check']}")
        
        if validation_result['issues']:
            log("[VALIDATION] Issues found:")
            for issue in validation_result['issues']:
                log(f"  - {issue}")
        
        if not validation_result['valid']:
            log("[VALIDATION] Validation failed - see issues above")
            sys.exit(1)
        else:
            log("[VALIDATION] All checks passed")

        log("[SUCCESS] Step 01 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)