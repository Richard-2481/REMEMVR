#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step03
Step Name: merge_analysis_dataset
RQ: results/ch7/7.4.2
Generated: 2026-01-06

PURPOSE:
Merge domain theta scores with BVMT scores to create analysis-ready dataset

EXPECTED INPUTS:
  - results/ch7/7.4.2/data/step01_domain_theta_scores.csv
    Columns: ['UID', 'Where_mean', 'What_mean']
    Format: Domain-specific theta scores from Ch5 analyses
    Expected rows: ~100

  - results/ch7/7.4.2/data/step02_bvmt_scores.csv
    Columns: ['UID', 'bvmt_total', 'bvmt_pct_ret']
    Format: BVMT Total Recall and percent retention scores per participant
    Expected rows: ~100

EXPECTED OUTPUTS:
  - results/ch7/7.4.2/data/step03_analysis_dataset.csv
    Columns: ['UID', 'bvmt_total', 'bvmt_pct_ret', 'Where_mean', 'What_mean']
    Format: Complete analysis dataset with all variables
    Expected rows: ~100

VALIDATION CRITERIA:
  - Exactly 100 participants retained (no data loss)
  - No duplicate UIDs
  - No missing values in analysis variables
  - All variables within expected ranges

g_code REASONING:
- Approach: Inner join on UID column to merge domain theta scores with BVMT data
- Why this approach: Ensures only participants with both VR and cognitive data are included
- Data flow: Load both files -> merge on UID -> validate completeness -> save result
- Expected performance: ~seconds (small dataframes)

IMPLEMENTATION NOTES:
- Analysis tool: Custom merge implementation (tools.data.merge_theta_cognitive has parameter mismatch)
- Validation tool: tools.validation.validate_dataframe_structure
- Parameters: Inner join ensures no data loss from missing participants
- Known issue: Signature mismatch found in Layer 4b validation - using custom implementation per gcode_lessons.md #9
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/ch7/7.4.2/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = 7.4.2/
#   parents[2] = ch7/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import validation tool
from tools.validation import validate_dataframe_structure

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.4.2 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step03_merge_analysis_dataset.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step03_analysis_dataset.csv
#   CORRECT: data/step01_domain_theta_scores.csv
#   WRONG:   results/analysis_dataset.csv  (wrong folder + no prefix)
#   WRONG:   data/analysis_dataset.csv     (missing step prefix)
#   WRONG:   logs/step03_merged_data.csv   (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
    print(msg)

# =============================================================================
# Custom Merge Function (Parameter Mismatch Workaround)
# =============================================================================

def merge_theta_cognitive_custom(theta_df, cognitive_df, uid_col='UID'):
    """
    Custom merge function to work around parameter mismatch in tools.data.merge_theta_cognitive.
    
    Issue: tools.data.merge_theta_cognitive has parameters (theta_df, cognitive_df, how)
    But 4_analysis.yaml expects (theta_df, cognitive_df, uid_col).
    
    Solution: Implement custom merge logic per gcode_lessons.md #9.
    """
    log(f"[MERGE] Custom merge on column: {uid_col}")
    log(f"[MERGE] Theta data shape: {theta_df.shape}")
    log(f"[MERGE] Cognitive data shape: {cognitive_df.shape}")
    
    # Verify UID column exists in both dataframes
    if uid_col not in theta_df.columns:
        raise ValueError(f"UID column '{uid_col}' not found in theta_df. Available: {theta_df.columns.tolist()}")
    if uid_col not in cognitive_df.columns:
        raise ValueError(f"UID column '{uid_col}' not found in cognitive_df. Available: {cognitive_df.columns.tolist()}")
    
    # Check for duplicate UIDs
    theta_dups = theta_df[uid_col].duplicated().sum()
    cognitive_dups = cognitive_df[uid_col].duplicated().sum()
    if theta_dups > 0:
        raise ValueError(f"Duplicate UIDs in theta_df: {theta_dups} duplicates found")
    if cognitive_dups > 0:
        raise ValueError(f"Duplicate UIDs in cognitive_df: {cognitive_dups} duplicates found")
    
    # Inner join to ensure complete data only
    merged_df = pd.merge(theta_df, cognitive_df, on=uid_col, how='inner')
    
    log(f"[MERGE] Merged shape: {merged_df.shape}")
    log(f"[MERGE] Participants retained: {len(merged_df)}")
    
    # Verify no data loss (should have exactly 100 participants)
    expected_n = 100
    if len(merged_df) != expected_n:
        log(f"[WARNING] Expected {expected_n} participants, got {len(merged_df)}")
    
    return merged_df

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 03: merge_analysis_dataset")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: Domain theta scores from Step 1 + BVMT scores from Step 2
        # Purpose: Merge into single analysis-ready dataset for correlation analysis

        log("[LOAD] Loading input data...")
        
        # Load Step 01 output: domain theta scores
        # Expected columns: UID, Where_mean, What_mean
        # Expected rows: ~100
        theta_input = pd.read_csv(RQ_DIR / "data/step01_domain_theta_scores.csv")
        log(f"[LOADED] step01_domain_theta_scores.csv ({len(theta_input)} rows, {len(theta_input.columns)} cols)")
        log(f"[COLUMNS] Theta data: {theta_input.columns.tolist()}")
        
        # Load Step 02 output: BVMT scores
        # Expected columns: UID, bvmt_total, bvmt_pct_ret
        # Expected rows: ~100
        bvmt_input = pd.read_csv(RQ_DIR / "data/step02_bvmt_scores.csv")
        log(f"[LOADED] step02_bvmt_scores.csv ({len(bvmt_input)} rows, {len(bvmt_input.columns)} cols)")
        log(f"[COLUMNS] BVMT data: {bvmt_input.columns.tolist()}")

        # =========================================================================
        # STEP 2: Run Analysis Tool (Custom Merge Implementation)
        # =========================================================================
        # Tool: Custom merge_theta_cognitive (workaround for parameter mismatch)
        # What it does: Inner join on UID to create complete analysis dataset
        # Expected output: 100 rows with UID, bvmt_total, Where_mean, What_mean

        log("[ANALYSIS] Running custom merge_theta_cognitive...")
        merged_data = merge_theta_cognitive_custom(
            theta_df=theta_input,
            cognitive_df=bvmt_input,
            uid_col="UID"  # Standard UID column for merge
        )
        log("[DONE] Merge analysis complete")

        # =========================================================================
        # STEP 3: Save Analysis Outputs
        # =========================================================================
        # These outputs will be used by: Step 04 correlation analysis and Step 05 Steiger test

        log(f"[SAVE] Saving step03_analysis_dataset.csv...")
        # Output: step03_analysis_dataset.csv
        # Contains: Complete analysis dataset with domain theta and BVMT scores
        # Columns: UID, bvmt_total, Where_mean, What_mean
        merged_data.to_csv(RQ_DIR / "data/step03_analysis_dataset.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step03_analysis_dataset.csv ({len(merged_data)} rows, {len(merged_data.columns)} cols)")
        
        # Verify expected column order
        expected_columns = ["UID", "bvmt_total", "bvmt_pct_ret", "Where_mean", "What_mean"]
        actual_columns = merged_data.columns.tolist()
        if actual_columns == expected_columns:
            log("[VERIFY] Output columns match expected format")
        else:
            log(f"[WARNING] Column order mismatch. Expected: {expected_columns}, Actual: {actual_columns}")

        # =========================================================================
        # STEP 4: Run Validation Tool
        # =========================================================================
        # Tool: validate_dataframe_structure (substitute for missing validate_data_completeness)
        # Validates: Complete dataset structure and data integrity
        # Threshold: 100 participants expected, no missing data allowed

        log("[VALIDATION] Running validate_dataframe_structure...")
        validation_result = validate_dataframe_structure(
            merged_data,
            expected_columns=expected_columns,
            expected_rows=100
        )

        # Report validation results
        # Expected: Successful validation with 100 complete participants
        if isinstance(validation_result, dict):
            for key, value in validation_result.items():
                log(f"[VALIDATION] {key}: {value}")
        else:
            log(f"[VALIDATION] {validation_result}")

        # Additional manual validation checks
        log("[VALIDATION] Additional checks...")
        
        # Check for exactly 100 participants (no data loss)
        if len(merged_data) == 100:
            log("[VALIDATION] Participant count: PASS (100 retained)")
        else:
            log(f"[VALIDATION] Participant count: WARNING ({len(merged_data)} instead of 100)")
        
        # Check for no duplicate UIDs
        duplicate_count = merged_data['UID'].duplicated().sum()
        if duplicate_count == 0:
            log("[VALIDATION] Duplicate UIDs: PASS (none found)")
        else:
            log(f"[VALIDATION] Duplicate UIDs: FAIL ({duplicate_count} duplicates)")
            
        # Check for missing values
        missing_count = merged_data.isnull().sum().sum()
        if missing_count == 0:
            log("[VALIDATION] Missing values: PASS (none found)")
        else:
            log(f"[VALIDATION] Missing values: FAIL ({missing_count} missing)")
            
        # Check variable ranges
        # BVMT range: [0, 36] (theoretical max)
        bvmt_min, bvmt_max = merged_data['bvmt_total'].min(), merged_data['bvmt_total'].max()
        if 0 <= bvmt_min and bvmt_max <= 36:
            log(f"[VALIDATION] BVMT range: PASS ({bvmt_min:.1f} to {bvmt_max:.1f})")
        else:
            log(f"[VALIDATION] BVMT range: WARNING ({bvmt_min:.1f} to {bvmt_max:.1f}, expected [0,36])")
            
        # Theta range: [-3, 3] (typical IRT range)
        where_min, where_max = merged_data['Where_mean'].min(), merged_data['Where_mean'].max()
        what_min, what_max = merged_data['What_mean'].min(), merged_data['What_mean'].max()
        if -3 <= where_min and where_max <= 3:
            log(f"[VALIDATION] Where theta range: PASS ({where_min:.2f} to {where_max:.2f})")
        else:
            log(f"[VALIDATION] Where theta range: WARNING ({where_min:.2f} to {where_max:.2f}, expected [-3,3])")
        if -3 <= what_min and what_max <= 3:
            log(f"[VALIDATION] What theta range: PASS ({what_min:.2f} to {what_max:.2f})")
        else:
            log(f"[VALIDATION] What theta range: WARNING ({what_min:.2f} to {what_max:.2f}, expected [-3,3])")

        log("[SUCCESS] Step 03 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)