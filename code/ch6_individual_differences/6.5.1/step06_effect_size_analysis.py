#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step06
Step Name: effect_size_analysis
RQ: results/ch7/7.5.1
Generated: 2026-01-06T21:15:00Z

PURPOSE:
Compute comprehensive effect sizes with bootstrap confidence intervals including
Cohen's f² for model effect sizes, bootstrap effect sizes for R² and f² confidence 
intervals, and relative importance (semi-partial correlations) for predictor rankings.

EXPECTED INPUTS:
  - results/ch7/7.5.1/data/step04_regression_models.csv
    Columns: ['model', 'R2', 'adj_R2', 'F_stat', 'F_p', 'AIC', 'BIC', 'N']
    Format: Model comparison between control and full models
    Expected rows: 2 (Control and Full models)
    
  - results/ch7/7.5.1/data/step04_coefficients_ci.csv
    Columns: ['predictor', 'beta', 'se', 'ci_lower', 'ci_upper', 'p_uncorrected', 'p_bonferroni', 'p_fdr']
    Format: Regression coefficients with bootstrap CIs and dual p-values
    Expected rows: 4 (Age_z, Education_z, VR_Experience_z, Typical_Sleep_z)
    
  - results/ch7/7.5.1/data/step03_analysis_dataset.csv
    Columns: ['UID', 'theta_all', 'Education_z', 'VR_Experience_z', 'Typical_Sleep_z', 'Age_z']
    Format: Analysis-ready dataset with standardized predictors
    Expected rows: ~100 (complete cases participants)

EXPECTED OUTPUTS:
  - results/ch7/7.5.1/data/step06_effect_sizes.csv
    Columns: ['measure', 'value', 'ci_lower', 'ci_upper', 'interpretation', 'bootstrap_se']
    Format: Comprehensive effect size measures with CIs
    Expected rows: 3 (R2_full, Cohens_f2, f2_change)
    
  - results/ch7/7.5.1/data/step06_relative_importance.csv
    Columns: ['predictor', 'sr2', 'rank', 'percent_of_r2']
    Format: Predictor relative importance rankings
    Expected rows: 3 (main predictors: Education_z, VR_Experience_z, Typical_Sleep_z)

VALIDATION CRITERIA:
  - f2_column: Effect sizes within reasonable bounds (Cohen's f² guidelines)

g_code REASONING:
- Approach: Compute Cohen's f² from nested model R² values, bootstrap confidence intervals
  for robustness, and relative importance via semi-partial correlations
- Why this approach: Cohen's f² is standard for hierarchical regression effect sizes,
  bootstrap CIs provide robust confidence intervals without normality assumptions,
  semi-partial correlations rank predictor importance independent of other predictors
- Data flow: Load model results → compute Cohen's f² → bootstrap effect sizes → 
  compute relative importance → save outputs with interpretations
- Expected performance: ~30 seconds (1000 bootstrap iterations)

IMPLEMENTATION NOTES:
- Analysis tool: compute_cohens_f2 from tools.analysis_regression
- Validation tool: validate_effect_sizes from tools.validation  
- Parameters: 1000 bootstrap iterations with seed=42 for reproducibility
- Effect size interpretation: f² < 0.02 (negligible), 0.02-0.15 (small), 0.15-0.35 (medium), >0.35 (large)
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
import statsmodels.api as sm
from typing import Dict, List, Tuple, Any
import traceback

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = rqY/
#   parents[2] = chX/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import analysis tool
from tools.analysis_regression import compute_cohens_f2

# Import validation tool
from tools.validation import validate_effect_sizes

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/chX/rqY (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step06_effect_size_analysis.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step06_effect_sizes.csv
#   CORRECT: data/step06_relative_importance.csv
#   WRONG:   results/effect_sizes.csv  (wrong folder + no prefix)
#   WRONG:   data/effect_sizes.csv     (missing step prefix)
#   WRONG:   logs/step06_effect_sizes.csv  (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
    print(msg)

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 06: Effect Size Analysis")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: Model comparison results, coefficients with CIs, analysis dataset
        # Purpose: Compute comprehensive effect sizes with bootstrap confidence intervals

        log("[LOAD] Loading input data...")
        
        # Load model comparison results
        # Expected columns: ['model', 'R2', 'adj_R2', 'F_stat', 'F_p', 'AIC', 'BIC', 'N']
        # Expected rows: 2 (Control and Full models)
        models_df = pd.read_csv(RQ_DIR / "data/step04_regression_models.csv")
        log(f"[LOADED] step04_regression_models.csv ({len(models_df)} models, {len(models_df.columns)} cols)")
        
        # Load coefficient results with CIs
        # Expected columns: ['predictor', 'beta', 'se', 'ci_lower', 'ci_upper', 'p_uncorrected', 'p_bonferroni', 'p_fdr']
        # Expected rows: 4 (Age_z, Education_z, VR_Experience_z, Typical_Sleep_z)
        coeffs_df = pd.read_csv(RQ_DIR / "data/step04_coefficients_ci.csv")
        log(f"[LOADED] step04_coefficients_ci.csv ({len(coeffs_df)} predictors, {len(coeffs_df.columns)} cols)")
        
        # Load analysis dataset for bootstrap
        # Expected columns: ['UID', 'theta_all', 'Education_z', 'VR_Experience_z', 'Typical_Sleep_z', 'Age_z'] 
        # Expected rows: ~100 complete cases
        analysis_df = pd.read_csv(RQ_DIR / "data/step03_analysis_dataset.csv")
        log(f"[LOADED] step03_analysis_dataset.csv ({len(analysis_df)} participants, {len(analysis_df.columns)} cols)")

        # =========================================================================
        # STEP 2: Run Analysis Tool - Compute Cohen's f²
        # =========================================================================
        # Tool: compute_cohens_f2
        # What it does: Computes Cohen's f² effect size for comparing nested regression models
        # Expected output: Single f² value comparing full vs control model

        log("[ANALYSIS] Computing Cohen's f² effect size...")
        
        # Extract R² values from control and full models
        r2_control = models_df[models_df['model'] == 'Control']['R2'].iloc[0]
        r2_full = models_df[models_df['model'] == 'Full']['R2'].iloc[0]
        
        log(f"[INFO] Control model R² = {r2_control:.4f}")
        log(f"[INFO] Full model R² = {r2_full:.4f}")
        
        # Compute Cohen's f² using analysis tool
        cohens_f2 = compute_cohens_f2(r2_full=r2_full, r2_reduced=r2_control)
        
        # Compute f² change (additional measure)
        f2_change = (r2_full - r2_control) / (1 - r2_full) if r2_full < 1.0 else float('inf')
        
        log(f"[ANALYSIS] Cohen's f² = {cohens_f2:.4f}")
        log(f"[ANALYSIS] f² change = {f2_change:.4f}")

        # =========================================================================
        # STEP 3: Bootstrap Effect Sizes (1000 iterations, seed=42)
        # =========================================================================
        # Tool: Custom bootstrap implementation 
        # What it does: Generates robust confidence intervals for R² and f² measures
        # Expected output: Bootstrap distribution and 95% CIs

        log("[BOOTSTRAP] Computing bootstrap confidence intervals (1000 iterations, seed=42)...")
        
        # Effect size interpretation function
        def interpret_f2(f2_value):
            """Interpret Cohen's f² according to Cohen (1988) guidelines."""
            if f2_value >= 0.35:
                return "Large"
            elif f2_value >= 0.15:
                return "Medium" 
            elif f2_value >= 0.02:
                return "Small"
            else:
                return "Negligible"
        
        # Bootstrap parameters
        n_bootstrap = 1000
        np.random.seed(42)  # Reproducibility
        
        # Storage for bootstrap results
        r2_bootstrap = []
        f2_bootstrap = []
        
        # Prepare predictor matrix and outcome vector
        predictor_cols = ['Age_z', 'Education_z', 'VR_Experience_z', 'Typical_Sleep_z']
        
        for iteration in range(n_bootstrap):
            try:
                # Resample participants with replacement (bootstrap sample)
                boot_idx = np.random.choice(len(analysis_df), len(analysis_df), replace=True)
                boot_data = analysis_df.iloc[boot_idx]
                
                # Prepare data for regression
                X = boot_data[predictor_cols].values
                X_with_const = sm.add_constant(X)  # Add intercept
                y = boot_data['theta_all'].values
                
                # Fit full model
                model = sm.OLS(y, X_with_const).fit()
                
                # Store bootstrap statistics
                r2_boot = model.rsquared
                f2_boot = r2_boot / (1 - r2_boot) if r2_boot < 1.0 else float('inf')
                
                r2_bootstrap.append(r2_boot)
                f2_bootstrap.append(f2_boot)
                
            except Exception as e:
                # Skip failed bootstrap iterations (e.g., singular matrix)
                log(f"[BOOTSTRAP] Skipped iteration {iteration + 1}: {str(e)}")
                continue
        
        # Calculate 95% confidence intervals
        if len(r2_bootstrap) >= 50:  # Minimum valid bootstrap samples
            r2_ci = np.percentile(r2_bootstrap, [2.5, 97.5])
            f2_ci = np.percentile(f2_bootstrap, [2.5, 97.5])
            bootstrap_success = True
            log(f"[BOOTSTRAP] Completed: {len(r2_bootstrap)} valid samples")
        else:
            r2_ci = [np.nan, np.nan]
            f2_ci = [np.nan, np.nan] 
            bootstrap_success = False
            log(f"[BOOTSTRAP] WARNING: Only {len(r2_bootstrap)} valid samples - insufficient for CIs")

        # =========================================================================
        # STEP 4: Save Effect Size Analysis Outputs
        # =========================================================================
        # These outputs will be used by: Final interpretation and relative importance analysis

        # Prepare effect sizes with interpretation
        effect_sizes = [
            {
                'measure': 'R2_full',
                'value': r2_full,
                'ci_lower': r2_ci[0] if bootstrap_success else np.nan,
                'ci_upper': r2_ci[1] if bootstrap_success else np.nan,
                'interpretation': f"R² = {r2_full:.3f}",
                'bootstrap_se': np.std(r2_bootstrap) if bootstrap_success else np.nan
            },
            {
                'measure': 'Cohens_f2',
                'value': cohens_f2,
                'ci_lower': f2_ci[0] if bootstrap_success else np.nan,
                'ci_upper': f2_ci[1] if bootstrap_success else np.nan,
                'interpretation': interpret_f2(cohens_f2),
                'bootstrap_se': np.std(f2_bootstrap) if bootstrap_success else np.nan
            },
            {
                'measure': 'f2_change',
                'value': f2_change,
                'ci_lower': np.nan,  # Not bootstrapped separately
                'ci_upper': np.nan,
                'interpretation': interpret_f2(f2_change),
                'bootstrap_se': np.nan
            }
        ]
        
        # Save effect sizes
        log(f"[SAVE] Saving effect sizes...")
        effect_sizes_df = pd.DataFrame(effect_sizes)
        effect_sizes_df.to_csv(RQ_DIR / "data/step06_effect_sizes.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step06_effect_sizes.csv ({len(effect_sizes_df)} measures, {len(effect_sizes_df.columns)} cols)")
        
        # =========================================================================
        # STEP 5: Compute Relative Importance (Semi-Partial Correlations)
        # =========================================================================
        # Tool: Custom implementation using semi-partial r² 
        # What it does: Ranks predictors by unique contribution to variance explained
        # Expected output: Ranked list of main predictors with importance percentages

        log("[ANALYSIS] Computing relative importance via semi-partial correlations...")
        
        # Focus on main predictors (exclude Age_z control variable)
        main_predictors = coeffs_df[coeffs_df['predictor'].isin(['Education_z', 'VR_Experience_z', 'Typical_Sleep_z'])]
        
        # Compute semi-partial r² for each predictor  
        relative_importance = []
        for _, row in main_predictors.iterrows():
            # Semi-partial r² approximation from standardized beta
            # sr² ≈ β² × (1 - R²_full) for standardized predictors
            sr2 = (row['beta'] ** 2) * (1 - r2_full)
            
            relative_importance.append({
                'predictor': row['predictor'],
                'sr2': sr2,
                'rank': 0,  # Will be filled after sorting
                'percent_of_r2': (sr2 / r2_full * 100) if r2_full > 0 else 0.0
            })
        
        # Create DataFrame and rank by semi-partial r²
        rel_imp_df = pd.DataFrame(relative_importance)
        rel_imp_df['rank'] = rel_imp_df['sr2'].rank(method='dense', ascending=False).astype(int)
        rel_imp_df = rel_imp_df.sort_values('rank')
        
        # Save relative importance
        log(f"[SAVE] Saving relative importance rankings...")
        rel_imp_df.to_csv(RQ_DIR / "data/step06_relative_importance.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step06_relative_importance.csv ({len(rel_imp_df)} predictors, {len(rel_imp_df.columns)} cols)")

        # =========================================================================
        # STEP 6: Run Validation Tool
        # =========================================================================
        # Tool: validate_effect_sizes
        # Validates: Effect sizes are within reasonable bounds (Cohen's f² guidelines)
        # Threshold: f² values should be non-negative, finite, and interpretable

        log("[VALIDATION] Running validate_effect_sizes...")
        validation_result = validate_effect_sizes(
            effect_sizes_df=effect_sizes_df,
            f2_column='value'  # Column containing f² values to validate
        )

        # Report validation results
        # Expected: All effect sizes within valid bounds (non-negative, finite)
        if isinstance(validation_result, dict):
            for key, value in validation_result.items():
                log(f"[VALIDATION] {key}: {value}")
        else:
            log(f"[VALIDATION] {validation_result}")

        # Log summary results
        log("[SUMMARY] Effect Size Analysis Results:")
        log(f"  Cohen's f² = {cohens_f2:.4f} ({interpret_f2(cohens_f2)})")
        log(f"  R² full model = {r2_full:.4f}")
        log(f"  Bootstrap 95% CI: f² [{f2_ci[0]:.4f}, {f2_ci[1]:.4f}]" if bootstrap_success else "  Bootstrap CIs unavailable")
        
        log("[SUMMARY] Relative Importance Rankings:")
        for _, row in rel_imp_df.iterrows():
            log(f"  Rank {row['rank']}: {row['predictor']} (sr² = {row['sr2']:.4f}, {row['percent_of_r2']:.1f}% of R²)")

        log("[SUCCESS] Step 06 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)