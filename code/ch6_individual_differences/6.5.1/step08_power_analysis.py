#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step08
Step Name: power_analysis
RQ: results/ch7/7.5.1
Generated: 2026-01-06

PURPOSE:
Post-hoc power analysis and sensitivity testing for regression analysis

EXPECTED INPUTS:
  - results/ch7/7.5.1/data/step04_regression_models.csv
    Columns: ['model', 'R2', 'adj_R2', 'F_stat', 'F_p', 'AIC', 'BIC', 'N']
    Format: Hierarchical regression model comparison results
    Expected rows: 2 (Control and Full models)
    
  - results/ch7/7.5.1/data/step04_coefficients_ci.csv
    Columns: ['predictor', 'beta', 'se', 'ci_lower', 'ci_upper', 'p_uncorrected', 'p_bonferroni', 'p_fdr']
    Format: Regression coefficients with confidence intervals
    Expected rows: 4 (Age_z, Education_z, VR_Experience_z, Typical_Sleep_z)

EXPECTED OUTPUTS:
  - results/ch7/7.5.1/data/step08_power_analysis.csv
    Columns: ['test', 'observed_effect', 'alpha', 'power', 'interpretation']
    Format: Post-hoc power analysis results for overall model and individual predictors
    Expected rows: ~4 (overall model + 3 main predictors)
    
  - results/ch7/7.5.1/data/step08_sensitivity_analysis.csv
    Columns: ['alpha_level', 'min_f2', 'min_r2', 'min_effect_label']
    Format: Minimum detectable effect sizes at 80% power
    Expected rows: 2 (uncorrected and corrected alpha levels)

VALIDATION CRITERIA:
  - power: Values between 0 and 1 (probability range)

g_code REASONING:
- Approach: Compute post-hoc statistical power for observed effects using non-central F distribution
- Why this approach: Determines whether non-significant results may be due to insufficient power rather than true null effects
- Data flow: Load model results -> compute power for overall model -> approximate power for individual predictors -> sensitivity analysis for minimum detectable effects
- Expected performance: ~seconds (simple statistical calculations)

IMPLEMENTATION NOTES:
- Analysis tool: compute_post_hoc_power from tools.analysis_regression
- Validation tool: validate_probability_range from tools.validation
- Parameters: Uses observed R2 values and corrected alpha levels (0.05 uncorrected, 0.0167 for multiple comparisons)
- Key insight from lessons #17: Statsmodels attributes are numpy arrays, not pandas - use direct indexing
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = 7.5.1/
#   parents[2] = ch7/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import analysis tool
from tools.analysis_regression import compute_post_hoc_power

# Import validation tool
from tools.validation import validate_probability_range

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.5.1 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step08_power_analysis.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step08_power_analysis.csv
#   CORRECT: data/step08_sensitivity_analysis.csv
#   WRONG:   results/power_analysis.csv  (wrong folder + no prefix)
#   WRONG:   data/power_analysis.csv     (missing step prefix)
#   WRONG:   logs/step08_results.csv     (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
    print(msg)

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 08: power_analysis")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: Model comparison results and coefficient estimates from step04
        # Purpose: Extract sample size, number of predictors, and observed R2 for power computation

        log("[LOAD] Loading model results...")
        models_df = pd.read_csv(RQ_DIR / "data/step04_regression_models.csv")
        log(f"[LOADED] step04_regression_models.csv ({len(models_df)} rows, {len(models_df.columns)} cols)")
        
        log("[LOAD] Loading coefficient results...")
        coeffs_df = pd.read_csv(RQ_DIR / "data/step04_coefficients_ci.csv")
        log(f"[LOADED] step04_coefficients_ci.csv ({len(coeffs_df)} rows, {len(coeffs_df.columns)} cols)")

        # Extract full model parameters for power analysis
        full_model = models_df[models_df['model'] == 'Full'].iloc[0]
        n = int(full_model['N'])
        k_predictors = 4  # Age, Education, VR_Experience, Typical_Sleep
        r2_observed = full_model['R2']
        
        log(f"[EXTRACT] Full model parameters: N={n}, k={k_predictors}, R2={r2_observed:.4f}")

        # =========================================================================
        # STEP 2: Run Analysis Tool - Overall Model Power
        # =========================================================================
        # Tool: compute_post_hoc_power
        # What it does: Computes statistical power for observed effect using non-central F distribution
        # Expected output: Power values for both uncorrected and corrected alpha levels

        log("[ANALYSIS] Computing post-hoc power for overall model...")
        
        # Power for uncorrected alpha (standard significance threshold)
        power_uncorrected = compute_post_hoc_power(
            n=n,
            k_predictors=k_predictors,
            r2=r2_observed,
            alpha=0.05  # Standard uncorrected alpha level
        )
        log(f"[COMPUTED] Uncorrected power (alpha=0.05): {power_uncorrected:.4f}")
        
        # Power for corrected alpha (Bonferroni correction for 3 main predictors)
        power_corrected = compute_post_hoc_power(
            n=n,
            k_predictors=k_predictors,
            r2=r2_observed,
            alpha=0.0167  # 0.05/3 for Bonferroni correction
        )
        log(f"[COMPUTED] Corrected power (alpha=0.0167): {power_corrected:.4f}")

        # =========================================================================
        # STEP 3: Individual Predictor Power Analysis
        # =========================================================================
        # Approach: Approximate power for individual predictors using t-statistics
        # Rationale: Individual predictor significance tests use t-distribution

        log("[ANALYSIS] Computing approximate power for individual predictors...")
        
        from scipy import stats
        
        power_results = []
        
        # Overall model power (primary result)
        power_results.append({
            'test': 'Overall_model',
            'observed_effect': r2_observed,
            'alpha': 0.05,
            'power': power_uncorrected,
            'interpretation': 'Adequate' if power_uncorrected >= 0.8 else ('Marginal' if power_uncorrected >= 0.6 else 'Inadequate')
        })
        
        # Individual predictor power approximations for main predictors
        main_predictors = ['Education_z', 'VR_Experience_z', 'Typical_Sleep_z']
        
        for predictor in main_predictors:
            coef_row = coeffs_df[coeffs_df['predictor'] == predictor].iloc[0]
            
            # Extract coefficient statistics (using direct indexing as per lesson #17)
            beta = coef_row['beta']
            se = coef_row['se']
            p_uncorrected = coef_row['p_uncorrected']
            
            # Compute t-statistic
            t_stat = abs(beta / se)
            
            # Approximate effect size for individual predictor
            # Using standardized beta as approximate effect size
            effect_size = abs(beta)
            
            # Approximate power using t-distribution
            # For two-tailed test with corrected alpha
            alpha_corrected = 0.0167
            df = n - k_predictors - 1
            t_critical = stats.t.ppf(1 - alpha_corrected/2, df)
            
            # Power approximation: probability that |t| > t_critical given observed effect
            # Using non-central t-distribution approximation
            ncp = t_stat  # Non-centrality parameter approximation
            power_approx = 1 - stats.t.cdf(t_critical, df, loc=ncp) + stats.t.cdf(-t_critical, df, loc=ncp)
            
            power_results.append({
                'test': f'{predictor}_individual',
                'observed_effect': effect_size,
                'alpha': alpha_corrected,
                'power': power_approx,
                'interpretation': 'Adequate' if power_approx >= 0.8 else ('Marginal' if power_approx >= 0.6 else 'Inadequate')
            })
            
            log(f"[COMPUTED] {predictor} power: {power_approx:.4f} (effect size: {effect_size:.4f})")

        # =========================================================================
        # STEP 4: Sensitivity Analysis - Minimum Detectable Effects
        # =========================================================================
        # Tool: statsmodels FTestAnovaPower for minimum effect size at 80% power
        # Purpose: Determine smallest effects that could be reliably detected with current sample size

        log("[ANALYSIS] Computing sensitivity analysis - minimum detectable effects...")
        
        from statsmodels.stats.power import FTestAnovaPower
        
        power_test = FTestAnovaPower()
        
        # Minimum Cohen's f² for 80% power at different alpha levels
        min_f2_uncorrected = power_test.solve_power(
            effect_size=None,  # Solve for this
            nobs=n,
            alpha=0.05,
            power=0.8
        )
        
        min_f2_corrected = power_test.solve_power(
            effect_size=None,  # Solve for this
            nobs=n,
            alpha=0.0167,
            power=0.8
        )
        
        # Convert Cohen's f² to R² for interpretation
        # R² = f² / (1 + f²)
        min_r2_uncorrected = min_f2_uncorrected / (1 + min_f2_uncorrected)
        min_r2_corrected = min_f2_corrected / (1 + min_f2_corrected)
        
        # Effect size interpretation (Cohen's conventions)
        def interpret_f2(f2):
            if f2 >= 0.35:
                return "Large"
            elif f2 >= 0.15:
                return "Medium"
            elif f2 >= 0.02:
                return "Small"
            else:
                return "Negligible"
        
        sensitivity_results = [
            {
                'alpha_level': 0.05,
                'min_f2': min_f2_uncorrected,
                'min_r2': min_r2_uncorrected,
                'min_effect_label': interpret_f2(min_f2_uncorrected)
            },
            {
                'alpha_level': 0.0167,
                'min_f2': min_f2_corrected,
                'min_r2': min_r2_corrected,
                'min_effect_label': interpret_f2(min_f2_corrected)
            }
        ]
        
        log(f"[COMPUTED] Minimum detectable f² (alpha=0.05): {min_f2_uncorrected:.4f} ({interpret_f2(min_f2_uncorrected)} effect)")
        log(f"[COMPUTED] Minimum detectable f² (alpha=0.0167): {min_f2_corrected:.4f} ({interpret_f2(min_f2_corrected)} effect)")

        # =========================================================================
        # STEP 5: Save Analysis Outputs
        # =========================================================================
        # These outputs will be used by: Final results reporting and interpretation

        log("[SAVE] Saving power analysis results...")
        # Output: step08_power_analysis.csv
        # Contains: Post-hoc power for overall model and individual predictors
        # Columns: ['test', 'observed_effect', 'alpha', 'power', 'interpretation']
        power_df = pd.DataFrame(power_results)
        power_df.to_csv(RQ_DIR / "data/step08_power_analysis.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step08_power_analysis.csv ({len(power_df)} rows, {len(power_df.columns)} cols)")

        log("[SAVE] Saving sensitivity analysis results...")
        # Output: step08_sensitivity_analysis.csv
        # Contains: Minimum detectable effect sizes at 80% power for both alpha levels
        # Columns: ['alpha_level', 'min_f2', 'min_r2', 'min_effect_label']
        sensitivity_df = pd.DataFrame(sensitivity_results)
        sensitivity_df.to_csv(RQ_DIR / "data/step08_sensitivity_analysis.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step08_sensitivity_analysis.csv ({len(sensitivity_df)} rows, {len(sensitivity_df.columns)} cols)")

        # =========================================================================
        # STEP 6: Run Validation Tool
        # =========================================================================
        # Tool: validate_probability_range
        # Validates: Power values are between 0 and 1 (valid probabilities)
        # Note: Based on lesson #16, using manual validation since custom range parameter not supported

        log("[VALIDATION] Running power value validation...")
        
        # Manual validation of power values (0-1 range)
        power_values = power_df['power']
        within_bounds = power_values.between(0, 1)
        validation_result = {
            'valid': within_bounds.all(),
            'out_of_bounds_count': (~within_bounds).sum(),
            'min_value': power_values.min(),
            'max_value': power_values.max()
        }

        # Report validation results
        # Expected: All power values between 0 and 1 (valid probability range)
        if validation_result['valid']:
            log(f"[VALIDATION] PASS: All power values within valid range [0,1]")
            log(f"[VALIDATION] Range: [{validation_result['min_value']:.4f}, {validation_result['max_value']:.4f}]")
        else:
            log(f"[VALIDATION] FAIL: {validation_result['out_of_bounds_count']} power values out of bounds")
            log(f"[VALIDATION] Range: [{validation_result['min_value']:.4f}, {validation_result['max_value']:.4f}]")

        log("[SUCCESS] Step 08 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)