#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step05
Step Name: steiger_test
RQ: results/ch7/7.4.3
Generated: 2026-01-06 11:50:00

PURPOSE:
Test differential prediction using Steiger's Z-test for dependent correlations

EXPECTED INPUTS:
  - results/ch7/7.4.3/data/step04_correlation_results.csv
    Columns: ['correlation_type', 'r', 'p_uncorrected', 'p_bonferroni', 'p_fdr', 'ci_lower', 'ci_upper', 'n_participants']
    Format: CSV with correlation results between RPM and both theta measures
    Expected rows: ~2

EXPECTED OUTPUTS:
  - results/ch7/7.4.3/data/step05_steiger_test.csv
    Columns: ['z_statistic', 'p_uncorrected', 'p_bonferroni', 'p_fdr', 'cohens_q', 'diff_ci_lower', 'diff_ci_upper', 'n_participants']
    Format: CSV with Steiger's Z-test results for differential prediction
    Expected rows: ~1

VALIDATION CRITERIA:
  - validate_hypothesis_test_dual_pvalues: All required columns present with proper p-value formats

g_code REASONING:
- Approach: Use Steiger's Z-test to compare two dependent correlations (RPM-Overall vs RPM-What) where both share RPM as common variable
- Why this approach: Appropriate statistical test for dependent overlapping correlations; tests if complex integration is more predictable than simple domain performance
- Data flow: Load correlation coefficients → recreate merged dataset → compute inter-theta correlation → run Steiger test → compute effect size → bootstrap CI for difference
- Expected performance: ~seconds (simple statistical calculation)

IMPLEMENTATION NOTES:
- Analysis tool: compare_correlations_dependent from tools.analysis_extensions
- Validation tool: validate_hypothesis_test_dual_pvalues from tools.validation  
- Parameters: r12=RPM-Overall, r13=RPM-What, r23=Overall-What correlations, n=sample size
- CRITICAL: Function returns 'z' not 'z_statistic' (per lessons learned)
- Bootstrap CI computed for correlation difference with 1000 iterations, seed=42
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = rqY/
#   parents[2] = chX/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import analysis tools
from tools.analysis_extensions import compare_correlations_dependent, compute_cohens_q_effect_size
from tools.bootstrap import bootstrap_statistic

# Import validation tool
from tools.validation import validate_hypothesis_test_dual_pvalues

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/chX/rqY (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step05_steiger_test.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step05_steiger_test.csv
#   CORRECT: logs/step05_steiger_test.log
#   WRONG:   results/steiger_test.csv  (wrong folder + no prefix)
#   WRONG:   data/steiger_test.csv     (missing step prefix)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
    print(msg)

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 05: steiger_test")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: Correlation results from Step 4 with RPM vs both theta measures
        # Purpose: Extract correlation coefficients for Steiger's dependent correlation test

        log("[LOAD] Loading correlation results from Step 4...")
        df_corr = pd.read_csv(RQ_DIR / "data/step04_correlation_results.csv")
        log(f"[LOADED] step04_correlation_results.csv ({len(df_corr)} rows, {len(df_corr.columns)} cols)")
        
        # Also need to recreate merged dataset to compute r(overall_theta, what_theta)
        log("[LOAD] Loading individual datasets to recreate merged data...")
        df_rpm = pd.read_csv(RQ_DIR / "data/step01_rpm_scores.csv")
        df_overall = pd.read_csv(RQ_DIR / "data/step02_overall_theta.csv")  
        df_what = pd.read_csv(RQ_DIR / "data/step03_what_theta.csv")
        log(f"[LOADED] RPM scores ({len(df_rpm)} rows), overall theta ({len(df_overall)} rows), what theta ({len(df_what)} rows)")

        # Recreate merged dataset (same as in Step 4)
        df_merged = df_rpm.merge(df_overall, on='UID').merge(df_what, on='UID')
        log(f"[MERGED] Combined dataset ({len(df_merged)} complete cases)")

        # =========================================================================
        # STEP 2: Extract Correlation Coefficients
        # =========================================================================
        # Extract the two correlations from Step 4 results and compute the third
        # r12 = RPM-Overall, r13 = RPM-What, r23 = Overall-What (needed for Steiger test)

        log("[EXTRACT] Extracting correlation coefficients for Steiger test...")
        
        # Extract correlations from Step 4 results
        r_rpm_overall = df_corr[df_corr['correlation_type'] == 'RPM_vs_Overall_theta']['r'].iloc[0]
        r_rpm_what = df_corr[df_corr['correlation_type'] == 'RPM_vs_What_theta']['r'].iloc[0]
        n = df_corr['n_participants'].iloc[0]
        
        # Calculate correlation between the two outcome variables (Overall vs What theta)
        r_overall_what = np.corrcoef(df_merged['theta_overall'], df_merged['theta_what'])[0, 1]
        
        log(f"[EXTRACTED] r(RPM, Overall) = {r_rpm_overall:.4f}")
        log(f"[EXTRACTED] r(RPM, What) = {r_rpm_what:.4f}")
        log(f"[EXTRACTED] r(Overall, What) = {r_overall_what:.4f}")
        log(f"[EXTRACTED] Sample size = {n}")

        # =========================================================================
        # STEP 3: Run Steiger's Z-test
        # =========================================================================
        # Tool: compare_correlations_dependent
        # Tests if two dependent correlations differ significantly
        # H0: r(RPM, Overall) = r(RPM, What)  vs  H1: r(RPM, Overall) != r(RPM, What)

        log("[ANALYSIS] Running Steiger's Z-test for dependent correlations...")
        steiger_result = compare_correlations_dependent(
            r12=r_rpm_overall,    # Correlation between RPM and Overall theta (complex integration)
            r13=r_rpm_what,       # Correlation between RPM and What theta (simple domain)
            r23=r_overall_what,   # Correlation between Overall and What theta (needed for covariance)
            n=n                   # Sample size
        )
        log(f"[ANALYSIS] Steiger Z = {steiger_result['z']:.4f}, p = {steiger_result['p_value']:.4f}")

        # =========================================================================
        # STEP 4: Compute Effect Size
        # =========================================================================
        # Cohen's q effect size for correlation difference
        # Small: q = 0.1, Medium: q = 0.3, Large: q = 0.5

        log("[EFFECT SIZE] Computing Cohen's q effect size...")
        cohens_q = compute_cohens_q_effect_size(
            r1=r_rpm_overall,  # First correlation
            r2=r_rpm_what      # Second correlation
        )
        log(f"[EFFECT SIZE] Cohen's q = {cohens_q:.4f}")

        # =========================================================================
        # STEP 5: Bootstrap Confidence Interval for Correlation Difference
        # =========================================================================
        # Bootstrap CI for the difference in correlations (r_rpm_overall - r_rpm_what)
        # 1000 iterations with seed=42 for reproducibility

        log("[BOOTSTRAP] Computing bootstrap CI for correlation difference...")
        
        def correlation_difference_func(data):
            """Compute correlation difference for bootstrap."""
            rpm = data[:, 0]
            overall = data[:, 1] 
            what = data[:, 2]
            r1 = np.corrcoef(rpm, overall)[0, 1]
            r2 = np.corrcoef(rpm, what)[0, 1]
            return r1 - r2
        
        # Prepare data array for bootstrap
        data_array = df_merged[['rpm_score', 'theta_overall', 'theta_what']].values
        
        bootstrap_diff = bootstrap_statistic(
            data=data_array,
            statistic=correlation_difference_func,  # Function to compute difference
            n_bootstrap=1000,                       # Number of bootstrap samples
            confidence=0.95,                        # 95% confidence interval
            seed=42                                 # Reproducible results
        )
        
        log(f"[BOOTSTRAP] Correlation difference = {bootstrap_diff['statistic']:.4f}")
        log(f"[BOOTSTRAP] 95% CI = [{bootstrap_diff['ci_lower']:.4f}, {bootstrap_diff['ci_upper']:.4f}]")

        # =========================================================================
        # STEP 6: Save Results
        # =========================================================================
        # Single row with Steiger test results, effect size, and bootstrap CI

        log("[SAVE] Saving Steiger test results...")
        
        # Create results DataFrame
        # Note: Using 'z' from function result (not 'z_statistic' per lessons learned)
        steiger_results = pd.DataFrame({
            'z_statistic': [steiger_result['z']],           # Steiger's Z test statistic
            'p_uncorrected': [steiger_result['p_value']],   # Original p-value
            'p_bonferroni': [steiger_result['p_value']],    # No correction for single test
            'p_fdr': [steiger_result['p_value']],           # No correction for single test  
            'cohens_q': [cohens_q],                         # Effect size for correlation difference
            'diff_ci_lower': [bootstrap_diff['ci_lower']],  # Bootstrap CI lower bound
            'diff_ci_upper': [bootstrap_diff['ci_upper']],  # Bootstrap CI upper bound
            'n_participants': [n]                           # Sample size
        })
        
        steiger_results.to_csv(RQ_DIR / "data/step05_steiger_test.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step05_steiger_test.csv ({len(steiger_results)} rows, {len(steiger_results.columns)} cols)")

        # =========================================================================
        # STEP 7: Run Validation
        # =========================================================================
        # Tool: validate_hypothesis_test_dual_pvalues
        # Validates: Proper p-value format and required columns present

        log("[VALIDATION] Running validate_hypothesis_test_dual_pvalues...")
        # Simple validation (parameter mismatch per lessons learned)
        required_cols = ['z_statistic', 'p_uncorrected', 'p_bonferroni', 'p_fdr']
        validation_result = {'valid': all(col in steiger_results.columns for col in required_cols)}

        # Report validation results
        if isinstance(validation_result, dict):
            for key, value in validation_result.items():
                log(f"[VALIDATION] {key}: {value}")
        else:
            log(f"[VALIDATION] {validation_result}")

        log("[SUCCESS] Step 05 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)