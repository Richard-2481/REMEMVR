#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step06
Step Name: step06_check_assumptions
RQ: results/ch7/7.4.3
Generated: 2026-01-06 16:50:00

PURPOSE:
Validate assumptions for correlation analysis and identify potential outliers

EXPECTED INPUTS:
  - results/ch7/7.4.3/data/step01_rpm_scores.csv
    Columns: ['UID', 'rpm_score', 'rpm_standardized']
    Format: CSV with RPM scores and standardization
    Expected rows: ~100

  - results/ch7/7.4.3/data/step02_overall_theta.csv
    Columns: ['UID', 'theta_overall', 'se_overall']  
    Format: CSV with overall omnibus theta scores
    Expected rows: ~100

  - results/ch7/7.4.3/data/step03_what_theta.csv
    Columns: ['UID', 'theta_what', 'se_what']
    Format: CSV with What-domain theta scores  
    Expected rows: ~100

EXPECTED OUTPUTS:
  - results/ch7/7.4.3/data/step06_assumption_checks.csv
    Columns: ['variable', 'normality_p', 'outlier_count', 'assumption_met', 'remedial_action']
    Format: CSV with assumption validation results
    Expected rows: ~4 (3 univariate + 1 multivariate)

VALIDATION CRITERIA:
  - normality_p > 0.05 for each variable (Shapiro-Wilk test)
  - outlier_count <= 5 for conservative threshold
  - multivariate outliers: Mahalanobis distance > chi-square critical value

g_code REASONING:
- Approach: Test correlation assumptions (normality, outlier detection) for RPM vs theta correlations
- Why this approach: Correlation analysis requires approximately normal distributions and outlier sensitivity
- Data flow: Load merged dataset -> test normality -> detect univariate/multivariate outliers -> determine remedial actions
- Expected performance: ~seconds (statistical tests are fast)

IMPLEMENTATION NOTES:
- Analysis tool: scipy.stats.shapiro for normality testing  
- Validation tool: tools.validation.validate_data_format for output format validation
- Parameters: alpha=0.05, outlier_threshold=3.29 (conservative z-score cutoff)
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = rqY/
#   parents[2] = chX/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import analysis tools
from scipy.stats import shapiro, zscore, chi2
from scipy.spatial.distance import mahalanobis

# Import validation tool
from tools.validation import validate_data_format

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/chX/rqY (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step06_check_assumptions.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step06_assumption_checks.csv
#   CORRECT: data/step03_theta_scores.csv
#   WRONG:   results/assumption_checks.csv     (wrong folder + no prefix)
#   WRONG:   data/assumption_checks.csv        (missing step prefix)
#   WRONG:   logs/step06_assumptions.csv       (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
    print(msg)

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 6: Check Assumptions")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: Merged data with RPM scores and both theta measures
        # Purpose: Test correlation assumptions (normality, outlier detection)

        log("[LOAD] Loading input data...")
        
        # Load RPM scores
        df_rpm = pd.read_csv(RQ_DIR / "data/step01_rpm_scores.csv")
        log(f"[LOADED] step01_rpm_scores.csv ({len(df_rpm)} rows, {len(df_rpm.columns)} cols)")
        
        # Load overall theta scores
        df_overall = pd.read_csv(RQ_DIR / "data/step02_overall_theta.csv")
        log(f"[LOADED] step02_overall_theta.csv ({len(df_overall)} rows, {len(df_overall.columns)} cols)")
        
        # Load What theta scores
        df_what = pd.read_csv(RQ_DIR / "data/step03_what_theta.csv")
        log(f"[LOADED] step03_what_theta.csv ({len(df_what)} rows, {len(df_what.columns)} cols)")
        
        # Merge data (inner join to keep complete cases only)
        df_merged = df_rpm.merge(df_overall, on='UID').merge(df_what, on='UID')
        log(f"[MERGED] Combined dataset ({len(df_merged)} rows with complete data)")

        # =========================================================================
        # STEP 2: Run Analysis Tool
        # =========================================================================
        # Tool: scipy.stats.shapiro (normality testing)
        # What it does: Tests null hypothesis that data comes from normal distribution
        # Expected output: Statistical test results and outlier detection

        variables = ['rpm_score', 'theta_overall', 'theta_what']
        alpha = 0.05
        outlier_threshold = 3.29  # Conservative z-score cutoff (p < 0.001)
        
        log("[ANALYSIS] Running assumption checks...")
        
        results = []
        
        # Test each variable for normality and outliers
        for var in variables:
            log(f"[TEST] Checking assumptions for {var}...")
            
            # Shapiro-Wilk normality test
            # scipy.stats.shapiro(x) -> Tuple[float, float]
            stat, p_value = shapiro(df_merged[var])
            log(f"[SHAPIRO] {var}: statistic={stat:.4f}, p={p_value:.6f}")
            
            # Univariate outlier detection (|z| > 3.29)
            z_scores = np.abs(zscore(df_merged[var]))
            outliers = np.sum(z_scores > outlier_threshold)
            log(f"[OUTLIERS] {var}: {outliers} outliers found (|z| > {outlier_threshold})")
            
            # Determine if assumption met and remedial action
            assumption_met = p_value > alpha and outliers <= 5  # Conservative threshold
            
            if not assumption_met:
                if p_value <= alpha:
                    remedial_action = "Use bootstrap CIs (already computed)"
                else:
                    remedial_action = "Consider outlier removal for sensitivity analysis"
            else:
                remedial_action = "No action needed"
            
            results.append({
                'variable': var,
                'normality_p': p_value,
                'outlier_count': outliers,
                'assumption_met': assumption_met,
                'remedial_action': remedial_action
            })
            
            log(f"[RESULT] {var}: assumption_met={assumption_met}, action={remedial_action}")
        
        # Multivariate outlier detection (Mahalanobis distance)
        log("[TEST] Checking multivariate outliers...")
        
        data_matrix = df_merged[variables].values
        mean_vec = np.mean(data_matrix, axis=0)
        cov_matrix = np.cov(data_matrix.T)
        
        try:
            inv_cov = np.linalg.inv(cov_matrix)
            mahal_distances = [mahalanobis(row, mean_vec, inv_cov) for row in data_matrix]
            
            # Chi-square critical value for 3 variables at p < 0.001
            critical_value = chi2.ppf(0.999, df=len(variables))
            multivariate_outliers = np.sum(np.array(mahal_distances) > critical_value)
            
            log(f"[MAHALANOBIS] {multivariate_outliers} multivariate outliers found (critical={critical_value:.3f})")
            
            results.append({
                'variable': 'multivariate',
                'normality_p': np.nan,
                'outlier_count': multivariate_outliers,
                'assumption_met': multivariate_outliers <= 3,  # Conservative threshold
                'remedial_action': "Report results with/without outliers" if multivariate_outliers > 3 else "No action needed"
            })
            
        except np.linalg.LinAlgError:
            log("[ERROR] Could not compute Mahalanobis distance - singular covariance matrix")
            results.append({
                'variable': 'multivariate',
                'normality_p': np.nan,
                'outlier_count': np.nan,
                'assumption_met': False,
                'remedial_action': "Could not compute - singular covariance matrix"
            })

        log("[DONE] Analysis complete")

        # =========================================================================
        # STEP 3: Save Analysis Outputs
        # =========================================================================
        # These outputs will be used by: Downstream interpretation and sensitivity analysis

        log("[SAVE] Saving assumption_checks.csv...")
        
        # Output: step06_assumption_checks.csv
        # Contains: Assumption test results and remedial action recommendations
        # Columns: variable, normality_p, outlier_count, assumption_met, remedial_action
        assumption_results = pd.DataFrame(results)
        assumption_results.to_csv(RQ_DIR / "data/step06_assumption_checks.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step06_assumption_checks.csv ({len(assumption_results)} rows, {len(assumption_results.columns)} cols)")

        # =========================================================================
        # STEP 4: Run Validation Tool
        # =========================================================================
        # Tool: tools.validation.validate_data_format
        # Validates: Output format has required columns
        # Threshold: All expected columns must be present

        log("[VALIDATION] Running validate_data_format...")
        
        # Expected columns for assumption checks output
        expected_columns = ['variable', 'normality_p', 'outlier_count', 'assumption_met', 'remedial_action']
        
        validation_result = validate_data_format(
            df=assumption_results,
            required_cols=expected_columns
        )

        # Report validation results
        # Expected: All required columns present in output
        if isinstance(validation_result, dict):
            for key, value in validation_result.items():
                log(f"[VALIDATION] {key}: {value}")
        else:
            log(f"[VALIDATION] {validation_result}")

        log("[SUCCESS] Step 6 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)