#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step04
Step Name: characterize_demographics
RQ: results/ch7/7.7.4
Generated: 2026-01-09

PURPOSE:
Compute descriptive statistics for false negative demographic characterization.
Compares false negative group to other participants across 6 demographic variables
to identify potential profile characteristics.

EXPECTED INPUTS:
  - results/ch7/7.7.4/data/step03_false_negatives.csv
    Columns: ['Age', 'Education', 'VR_Experience', 'NART_Score', 'RAVLT_Total', 'REMEMVR_theta']
    Format: False negative cases for demographic characterization
    Expected rows: ~6-10

  - results/ch7/7.7.4/data/step02_standardized_scores.csv
    Columns: ['Age', 'Education', 'VR_Experience', 'NART_Score']
    Format: All participants for comparison group
    Expected rows: 100

EXPECTED OUTPUTS:
  - results/ch7/7.7.4/data/step04_demographic_summary.csv
    Columns: ['Variable', 'False_Neg_N', 'False_Neg_Mean', 'False_Neg_SD',
              'Other_N', 'Other_Mean', 'Other_SD']
    Format: Descriptive statistics comparing false negatives to other participants
    Expected rows: 6 (Age, Education, VR_Experience, NART_Score, RAVLT_Total, REMEMVR_theta)

VALIDATION CRITERIA:
  - Summary has 6 rows (one per demographic variable)
  - False_Neg_N matches number of false negatives from step03
  - Other_N + False_Neg_N = 100 (total sample)
  - No missing values in summary statistics

g_code REASONING:
- Approach: Compute mean/SD for each group, combine into comparison table
- Why this approach: Standard descriptive statistics for group characterization
- Data flow: False negative subset + full sample → compute descriptives → compare
- Expected performance: <2 seconds

IMPLEMENTATION NOTES:
- Analysis tool: pandas.describe() for descriptive statistics
- Validation tool: validate_dataframe_structure from tools.validation
- Parameters: Include all demographic variables plus cognitive scores
- Groups: "False_Neg" (identified cases) vs "Other" (remaining participants)
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Any
import traceback

# Add project root to path for imports
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import validation tool
from tools.validation import validate_dataframe_structure

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.7.4
LOG_FILE = RQ_DIR / "logs" / "step04_characterize_demographics.log"
INPUT_FALSE_NEG = RQ_DIR / "data" / "step03_false_negatives.csv"
INPUT_ALL = RQ_DIR / "data" / "step02_standardized_scores.csv"
OUTPUT_FILE = RQ_DIR / "data" / "step04_demographic_summary.csv"

# Variables to characterize
DEMOGRAPHIC_VARS = ['Age', 'Education', 'VR_Experience', 'NART_Score', 'RAVLT_Total', 'RAVLT_Pct_Ret', 'REMEMVR_theta']

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()
    print(msg, flush=True)

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 04: characterize_demographics")

        # =========================================================================
        # STEP 1: Load False Negatives
        # =========================================================================
        log("[LOAD] Loading false negative cases from step03...")

        df_false_neg = pd.read_csv(INPUT_FALSE_NEG)
        log(f"[LOADED] False negatives: {len(df_false_neg)} cases")

        # =========================================================================
        # STEP 2: Load All Participants
        # =========================================================================
        log("[LOAD] Loading all participants from step02...")

        df_all = pd.read_csv(INPUT_ALL)
        log(f"[LOADED] All participants: {len(df_all)} cases")

        # =========================================================================
        # STEP 3: Create "Other" Group (non-false-negatives)
        # =========================================================================
        log("[ANALYSIS] Creating comparison group (non-false-negatives)...")

        # Extract UIDs of false negatives
        false_neg_uids = df_false_neg['UID'].tolist()
        log(f"[INFO] False negative UIDs: {false_neg_uids}")

        # Create "Other" group by excluding false negatives
        df_other = df_all[~df_all['UID'].isin(false_neg_uids)].copy()
        log(f"[CREATED] Other group: {len(df_other)} participants")

        # Verify counts
        if len(df_false_neg) + len(df_other) != len(df_all):
            log(f"[ERROR] Group counts don't sum to total: {len(df_false_neg)} + {len(df_other)} != {len(df_all)}")
            raise ValueError("Group assignment error - counts don't match")
        else:
            log(f"[PASS] Group counts verified: {len(df_false_neg)} + {len(df_other)} = {len(df_all)}")

        # =========================================================================
        # STEP 4: Compute Descriptive Statistics
        # =========================================================================
        log("[ANALYSIS] Computing descriptive statistics by group...")

        summary_rows = []

        for var in DEMOGRAPHIC_VARS:
            log(f"[INFO] Processing variable: {var}")

            # False negative group statistics
            fn_data = df_false_neg[var]
            fn_n = fn_data.count()  # Count non-missing
            fn_mean = fn_data.mean()
            fn_sd = fn_data.std()

            # Other group statistics
            other_data = df_other[var]
            other_n = other_data.count()
            other_mean = other_data.mean()
            other_sd = other_data.std()

            # Create summary row
            summary_row = {
                'Variable': var,
                'False_Neg_N': fn_n,
                'False_Neg_Mean': fn_mean,
                'False_Neg_SD': fn_sd,
                'Other_N': other_n,
                'Other_Mean': other_mean,
                'Other_SD': other_sd
            }
            summary_rows.append(summary_row)

            # Log comparison
            log(f"  False Neg: N={fn_n}, Mean={fn_mean:.2f}, SD={fn_sd:.2f}")
            log(f"  Other:     N={other_n}, Mean={other_mean:.2f}, SD={other_sd:.2f}")

        # =========================================================================
        # STEP 5: Create Summary DataFrame
        # =========================================================================
        log("[ANALYSIS] Creating demographic summary table...")

        df_summary = pd.DataFrame(summary_rows)
        log(f"[CREATED] Summary table: {len(df_summary)} rows, {len(df_summary.columns)} columns")

        # =========================================================================
        # STEP 6: Save Summary
        # =========================================================================
        log("[SAVE] Saving demographic summary...")

        df_summary.to_csv(OUTPUT_FILE, index=False, encoding='utf-8')
        log(f"[SAVED] {OUTPUT_FILE}")

        # =========================================================================
        # STEP 7: Validate Output
        # =========================================================================
        log("[VALIDATION] Running validate_dataframe_structure...")

        expected_columns = ['Variable', 'False_Neg_N', 'False_Neg_Mean', 'False_Neg_SD',
                          'Other_N', 'Other_Mean', 'Other_SD']

        validation_result = validate_dataframe_structure(
            df=df_summary,
            expected_rows=7,  # 7 demographic variables
            expected_columns=expected_columns
        )

        if validation_result.get('valid', False):
            log(f"[PASS] Demographic summary structure validated")
        else:
            log(f"[WARN] Validation: {validation_result.get('message', 'Unknown issue')}")

        # Check for missing values
        missing_count = df_summary.isnull().sum().sum()
        if missing_count > 0:
            log(f"[WARN] Summary contains {missing_count} missing values")
        else:
            log(f"[PASS] No missing values in summary")

        # Verify sample sizes
        total_false_neg = df_summary['False_Neg_N'].iloc[0]  # Should be same for all vars
        total_other = df_summary['Other_N'].iloc[0]

        if total_false_neg != len(df_false_neg):
            log(f"[WARN] False_Neg_N ({total_false_neg}) != actual false neg count ({len(df_false_neg)})")
        else:
            log(f"[PASS] False negative sample size verified: {total_false_neg}")

        if total_other != len(df_other):
            log(f"[WARN] Other_N ({total_other}) != actual other count ({len(df_other)})")
        else:
            log(f"[PASS] Other group sample size verified: {total_other}")

        log("[SUCCESS] Step 04 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)
