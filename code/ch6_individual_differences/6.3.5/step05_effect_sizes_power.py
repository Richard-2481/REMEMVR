#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step05
Step Name: effect_sizes_power
RQ: results/ch7/7.3.5
Generated: 2026-01-06T01:20:00Z

PURPOSE:
Compute effect sizes and post-hoc power analysis for group comparisons and correlations
from steps 03 and 04. Calculate Cohen's d for group differences and assess achieved power.

EXPECTED INPUTS:
  - results/ch7/7.3.5/data/step03_anova_results.csv
    Columns: ['DV', 'F_stat', 'eta_squared'] (plus other columns)
    Format: ANOVA F-statistics for effect size computation
    Expected rows: 3 (education, rpm, age)
  - results/ch7/7.3.5/data/step04_correlations.csv
    Columns: ['variable_pair', 'r'] (plus other columns)
    Format: Correlation coefficients for effect size computation
    Expected rows: 3 (residual vs education, rpm, age)
  - results/ch7/7.3.5/data/step02_calibration_groups.csv
    Columns: ['group', 'education', 'rpm', 'age'] (for pairwise effect sizes)
    Format: Raw group data for Cohen's d calculations
    Expected rows: ~100

EXPECTED OUTPUTS:
  - results/ch7/7.3.5/data/step05_effect_sizes.csv
    Columns: ['comparison', 'dv', 'cohens_d', 'd_ci_lower', 'd_ci_upper', 'classification', 'n1', 'n2']
    Format: Cohen's d for all pairwise group comparisons
    Expected rows: 9 (3 DVs × 3 pairwise comparisons)
  - results/ch7/7.3.5/data/step05_power_analysis.csv
    Columns: ['test_type', 'effect', 'alpha_corrected', 'achieved_power', 'effect_size', 'n']
    Format: Post-hoc power analysis for ANOVAs and correlations
    Expected rows: 6 (3 ANOVAs + 3 correlations)

VALIDATION CRITERIA:
  - f2_column: Cohen's f² values within expected ranges
  - Effect size classifications match Cohen's conventions

g_code REASONING:
- Approach: Compute Cohen's d for pairwise comparisons, Cohen's f² from eta², power analysis
- Why this approach: Standardized effect sizes allow interpretation beyond statistical significance
- Data flow: Load ANOVA/correlation results → compute effect sizes → calculate power → validate
- Expected performance: ~20 seconds

IMPLEMENTATION NOTES:
- Analysis tool: compute_cohens_f2 from tools.analysis_regression (actual signature used)
- Validation tool: validate_effect_sizes from tools.validation
- Parameters: alpha_corrected=0.0083 (Bonferroni), Cohen's conventions for interpretation
- Custom implementations: Cohen's d calculation, power analysis using statsmodels
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback
from scipy import stats
from statsmodels.stats.power import FTestAnovaPower, TTestPower

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = rqY/
#   parents[2] = chX/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import analysis tool
from tools.analysis_regression import compute_cohens_f2

# Import validation tool
from tools.validation import validate_effect_sizes

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.3.5 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step05_effect_sizes_power.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step05_lmm_model_comparison.csv
#   CORRECT: data/step03_theta_scores.csv
#   WRONG:   results/lmm_model_comparison.csv  (wrong folder + no prefix)
#   WRONG:   data/theta_scores.csv             (missing step prefix)
#   WRONG:   logs/step02_removed_items.csv     (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()
    print(msg, flush=True)

# =============================================================================
# Effect Size and Power Functions
# =============================================================================

def compute_cohens_d_pairwise(group1_data, group2_data):
    """
    Compute Cohen's d for two groups with pooled standard deviation.
    """
    n1, n2 = len(group1_data), len(group2_data)
    mean1, mean2 = np.mean(group1_data), np.mean(group2_data)
    var1, var2 = np.var(group1_data, ddof=1), np.var(group2_data, ddof=1)
    
    # Pooled standard deviation
    pooled_sd = np.sqrt(((n1 - 1) * var1 + (n2 - 1) * var2) / (n1 + n2 - 2))
    
    # Cohen's d
    cohens_d = (mean1 - mean2) / pooled_sd
    
    # Confidence interval using Hedges' g formula (approximate)
    se_d = np.sqrt((n1 + n2) / (n1 * n2) + cohens_d**2 / (2 * (n1 + n2)))
    ci_lower = cohens_d - 1.96 * se_d
    ci_upper = cohens_d + 1.96 * se_d
    
    return cohens_d, ci_lower, ci_upper

def classify_cohens_d(d):
    """Classify Cohen's d effect size."""
    abs_d = abs(d)
    if abs_d < 0.2:
        return 'negligible'
    elif abs_d < 0.5:
        return 'small'
    elif abs_d < 0.8:
        return 'medium'
    else:
        return 'large'

def eta_squared_to_cohens_f(eta_squared):
    """Convert eta-squared to Cohen's f for power analysis."""
    return np.sqrt(eta_squared / (1 - eta_squared))

def compute_achieved_power_anova(f_stat, df_between, df_within, alpha=0.05):
    """Compute achieved power for ANOVA F-test."""
    try:
        # Use F-distribution to compute power
        critical_f = stats.f.ppf(1 - alpha, df_between, df_within)
        power = 1 - stats.f.cdf(critical_f, df_between, df_within, f_stat * df_between)
        return max(0.0, min(1.0, power))  # Bound between 0 and 1
    except:
        return np.nan

def compute_achieved_power_correlation(r, n, alpha=0.05):
    """Compute achieved power for correlation test."""
    try:
        # Convert correlation to t-statistic
        t_stat = r * np.sqrt((n - 2) / (1 - r**2))
        critical_t = stats.t.ppf(1 - alpha/2, n - 2)  # Two-tailed
        power = 1 - (stats.t.cdf(critical_t, n - 2) - stats.t.cdf(-critical_t, n - 2))
        return max(0.0, min(1.0, power))
    except:
        return np.nan

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 5: Effect Sizes and Power Analysis")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: ANOVA results, correlation results, and raw group data
        # Purpose: Compute standardized effect sizes and assess achieved power

        log("[LOAD] Loading ANOVA results...")
        anova_df = pd.read_csv(RQ_DIR / "data" / "step03_anova_results.csv")
        log(f"[LOADED] step03_anova_results.csv ({len(anova_df)} rows, {len(anova_df.columns)} cols)")
        
        log("[LOAD] Loading correlation results...")
        corr_df = pd.read_csv(RQ_DIR / "data" / "step04_correlations.csv")
        log(f"[LOADED] step04_correlations.csv ({len(corr_df)} rows, {len(corr_df.columns)} cols)")
        
        log("[LOAD] Loading raw group data...")
        groups_df = pd.read_csv(RQ_DIR / "data" / "step02_calibration_groups.csv")
        log(f"[LOADED] step02_calibration_groups.csv ({len(groups_df)} rows, {len(groups_df.columns)} cols)")

        # =========================================================================
        # STEP 2: Compute Pairwise Effect Sizes (Cohen's d)
        # =========================================================================
        # Tool: Custom Cohen's d calculation (no single function in tools)
        # What it does: Compute standardized mean differences for all group pairs
        # Expected output: Cohen's d with confidence intervals for all comparisons

        log("[ANALYSIS] Computing pairwise Cohen's d effect sizes...")
        
        dependent_variables = ['education', 'rpm', 'age']
        group_names = groups_df['group'].unique()
        group_pairs = []
        
        # Generate all pairwise combinations
        from itertools import combinations
        for g1, g2 in combinations(group_names, 2):
            group_pairs.append((g1, g2))
        
        effect_size_results = []
        
        for dv in dependent_variables:
            for group1, group2 in group_pairs:
                log(f"[ANALYSIS] Computing Cohen's d: {group1} vs {group2} on {dv}...")
                
                # Get data for each group
                group1_data = groups_df[groups_df['group'] == group1][dv].dropna()
                group2_data = groups_df[groups_df['group'] == group2][dv].dropna()
                
                if len(group1_data) < 3 or len(group2_data) < 3:
                    log(f"[WARNING] Insufficient data for {group1} vs {group2} on {dv}")
                    continue
                
                # Compute Cohen's d
                cohens_d, ci_lower, ci_upper = compute_cohens_d_pairwise(group1_data, group2_data)
                classification = classify_cohens_d(cohens_d)
                
                effect_size_results.append({
                    'comparison': f"{group1}_vs_{group2}",
                    'dv': dv,
                    'cohens_d': cohens_d,
                    'd_ci_lower': ci_lower,
                    'd_ci_upper': ci_upper,
                    'classification': classification,
                    'n1': len(group1_data),
                    'n2': len(group2_data)
                })
                
                log(f"[DONE] {group1} vs {group2} on {dv}: d = {cohens_d:.3f} ({classification})")

        # =========================================================================
        # STEP 3: Compute Cohen's f² and Power Analysis
        # =========================================================================
        # Tool: compute_cohens_f2 from tools.analysis_regression
        # What it does: Convert eta-squared to Cohen's f² for ANOVAs
        # Expected output: Effect sizes and achieved power for all tests

        log("[ANALYSIS] Computing Cohen's f² and power analysis...")
        
        power_results = []
        alpha_corrected = 0.0083  # Bonferroni correction: 0.05/6
        
        # ANOVA effect sizes and power
        for _, row in anova_df.iterrows():
            dv = row['DV']
            eta_squared = row['eta_squared']
            f_stat = row['F_stat']
            
            if not np.isnan(eta_squared) and eta_squared > 0:
                # Convert eta² to Cohen's f using custom function (since tools function expects R²)
                cohens_f = eta_squared_to_cohens_f(eta_squared)
                
                # Compute achieved power (approximate)
                df_between = 2  # 3 groups - 1
                df_within = len(groups_df) - 3  # N - k
                achieved_power = compute_achieved_power_anova(f_stat, df_between, df_within, alpha_corrected)
                
                power_results.append({
                    'test_type': 'ANOVA',
                    'effect': dv,
                    'alpha_corrected': alpha_corrected,
                    'achieved_power': achieved_power,
                    'effect_size': cohens_f,
                    'n': len(groups_df)
                })
                
                log(f"[DONE] ANOVA {dv}: f = {cohens_f:.3f}, power = {achieved_power:.3f}")
        
        # Correlation effect sizes and power
        for _, row in corr_df.iterrows():
            variable_pair = row['variable_pair']
            r = row['r']
            
            if not np.isnan(r):
                # For correlations, effect size IS the correlation coefficient
                # Compute achieved power
                n = len(groups_df)  # Sample size
                achieved_power = compute_achieved_power_correlation(r, n, alpha_corrected)
                
                power_results.append({
                    'test_type': 'Correlation',
                    'effect': variable_pair,
                    'alpha_corrected': alpha_corrected,
                    'achieved_power': achieved_power,
                    'effect_size': abs(r),  # Use absolute value for effect size
                    'n': n
                })
                
                log(f"[DONE] Correlation {variable_pair}: r = {r:.3f}, power = {achieved_power:.3f}")

        # =========================================================================
        # STEP 4: Save Analysis Outputs
        # =========================================================================
        # These outputs will be used by: Final reporting and interpretation

        log("[SAVE] Saving effect sizes and power analysis...")
        
        # Save effect sizes (Cohen's d)
        if effect_size_results:
            effect_sizes_df = pd.DataFrame(effect_size_results)
        else:
            # Create empty DataFrame with expected columns if no results
            effect_sizes_df = pd.DataFrame(columns=['comparison', 'dv', 'cohens_d', 'd_ci_lower', 'd_ci_upper', 'classification', 'n1', 'n2'])
        
        effect_sizes_df.to_csv(RQ_DIR / "data" / "step05_effect_sizes.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step05_effect_sizes.csv ({len(effect_sizes_df)} rows, {len(effect_sizes_df.columns)} cols)")
        
        # Save power analysis
        if power_results:
            power_df = pd.DataFrame(power_results)
        else:
            # Create empty DataFrame with expected columns if no results
            power_df = pd.DataFrame(columns=['test_type', 'effect', 'alpha_corrected', 'achieved_power', 'effect_size', 'n'])
        
        power_df.to_csv(RQ_DIR / "data" / "step05_power_analysis.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step05_power_analysis.csv ({len(power_df)} rows, {len(power_df.columns)} cols)")

        # =========================================================================
        # STEP 5: Run Validation Tool
        # =========================================================================
        # Tool: validate_effect_sizes
        # Validates: Effect size ranges and classifications
        # Threshold: Check that effect sizes are within reasonable bounds

        log("[VALIDATION] Running validate_effect_sizes...")
        
        # For validation, create a temporary DataFrame with f² column
        # Convert Cohen's d to approximate f² for validation
        if not effect_sizes_df.empty:
            effect_sizes_validation = effect_sizes_df.copy()
            effect_sizes_validation['cohens_f2'] = (effect_sizes_validation['cohens_d'] ** 2) / 4  # Approximate conversion
        else:
            effect_sizes_validation = pd.DataFrame({'cohens_f2': []})
        
        validation_result = validate_effect_sizes(
            effect_sizes_df=effect_sizes_validation,
            f2_column='cohens_f2'
        )

        # Report validation results
        # Expected: Confirms effect sizes are within reasonable ranges
        if isinstance(validation_result, dict):
            for key, value in validation_result.items():
                log(f"[VALIDATION] {key}: {value}")
        else:
            log(f"[VALIDATION] {validation_result}")

        # Summary statistics
        log("[SUMMARY] Effect Size and Power Summary:")
        
        if not effect_sizes_df.empty:
            log("  Cohen's d Effect Sizes:")
            for classification in ['negligible', 'small', 'medium', 'large']:
                count = len(effect_sizes_df[effect_sizes_df['classification'] == classification])
                log(f"    {classification}: {count} comparisons")
        
        if not power_df.empty:
            log("  Achieved Power:")
            underpowered = len(power_df[power_df['achieved_power'] < 0.80])
            log(f"    Well-powered (>80%): {len(power_df) - underpowered} tests")
            log(f"    Underpowered (<80%): {underpowered} tests")

        log("[SUCCESS] Step 5 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)