#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step03
Step Name: anova_comparisons
RQ: results/ch7/7.3.5
Generated: 2026-01-06T01:10:00Z

PURPOSE:
Compare calibration groups on cognitive reserve indicators using one-way ANOVA
with Decision D068 dual p-value reporting (uncorrected + Bonferroni corrected).

EXPECTED INPUTS:
  - results/ch7/7.3.5/data/step02_calibration_groups.csv
    Columns: ['group', 'education', 'rpm', 'age'] (plus other columns)
    Format: CSV with calibration groups and cognitive reserve data
    Expected rows: ~100

EXPECTED OUTPUTS:
  - results/ch7/7.3.5/data/step03_anova_results.csv
    Columns: ['DV', 'F_stat', 'p_uncorrected', 'p_bonferroni', 'eta_squared', 'eta_ci_lower', 'eta_ci_upper']
    Format: ANOVA results with dual p-values per Decision D068
    Expected rows: 3 (education, rpm, age)
  - results/ch7/7.3.5/data/step03_assumption_checks.csv
    Columns: ['DV', 'test', 'statistic', 'p_value', 'assumption', 'status']
    Format: ANOVA assumption test results
    Expected rows: 6 (3 DVs × 2 tests)
  - results/ch7/7.3.5/data/step03_posthoc_comparisons.csv
    Columns: ['DV', 'comparison', 'mean_diff', 'se', 't_stat', 'p_uncorrected', 'p_tukey', 'sig_uncorrected', 'sig_tukey']
    Format: Tukey HSD post-hoc pairwise comparisons
    Expected rows: 9 (3 DVs × 3 comparisons)

VALIDATION CRITERIA:
  - required_terms: ['education', 'rpm', 'age']
  - alpha_bonferroni: 0.05/6 = 0.0083 (Bonferroni correction for 6 tests)

g_code REASONING:
- Approach: One-way ANOVA for each DV (education, rpm, age) comparing 3 calibration groups
- Why this approach: Tests whether cognitive reserve differs by confidence calibration quality
- Data flow: Load groups → run ANOVA for each DV → check assumptions → post-hoc tests → validate
- Expected performance: ~30 seconds

IMPLEMENTATION NOTES:
- Analysis tool: one_way_anova_d068 from tools.analysis_stats (actual signature used)
- Validation tool: validate_hypothesis_test_dual_pvalues from tools.validation
- Parameters: 3 DVs, Bonferroni correction with n_comparisons=6, post-hoc Tukey HSD
- Handles Decision D068: Dual p-values (uncorrected and Bonferroni corrected)
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback
from scipy import stats

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = rqY/
#   parents[2] = chX/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import analysis tool
from tools.analysis_stats import one_way_anova_d068

# Import validation tool
from tools.validation import validate_hypothesis_test_dual_pvalues

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.3.5 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step03_anova_comparisons.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step05_lmm_model_comparison.csv
#   CORRECT: data/step03_theta_scores.csv
#   WRONG:   results/lmm_model_comparison.csv  (wrong folder + no prefix)
#   WRONG:   data/theta_scores.csv             (missing step prefix)
#   WRONG:   logs/step02_removed_items.csv     (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()
    print(msg, flush=True)

# =============================================================================
# Custom ANOVA Function (handles signature mismatch from gcode_lessons.md)
# =============================================================================

def run_anova_d068_wrapper(df, group_col, dv_col, n_comparisons=6):
    """
    Wrapper for one_way_anova_d068 using actual signature.
    Based on gcode_lessons.md: Always use actual function signatures.
    """
    # Create groups list from DataFrame
    groups = []
    group_names = df[group_col].unique()
    for group_name in group_names:
        group_data = df[df[group_col] == group_name][dv_col].values
        groups.append(group_data)
    
    # Use actual function signature: groups, data, dv, between, correction, n_comparisons, post_hoc
    result = one_way_anova_d068(
        groups=groups,
        data=df,  # Full dataset for post-hoc
        dv=dv_col,
        between=group_col,
        correction='bonferroni',
        n_comparisons=n_comparisons,
        post_hoc='tukey'  # Changed from True to 'tukey' string
    )
    return result

def check_anova_assumptions(df, group_col, dv_col):
    """Check normality and homoscedasticity assumptions."""
    assumptions = []
    
    # Normality test per group (Shapiro-Wilk)
    groups = df[group_col].unique()
    normality_pvals = []
    
    for group in groups:
        group_data = df[df[group_col] == group][dv_col].dropna()
        if len(group_data) >= 3:  # Minimum for Shapiro-Wilk
            stat, p_val = stats.shapiro(group_data)
            normality_pvals.append(p_val)
    
    # Overall normality test result
    min_normality_p = min(normality_pvals) if normality_pvals else 0.0
    assumptions.append({
        'DV': dv_col,
        'test': 'shapiro_wilk',
        'statistic': 'min_group_p',
        'p_value': min_normality_p,
        'assumption': 'normality',
        'status': 'pass' if min_normality_p > 0.05 else 'violation'
    })
    
    # Levene's test for homoscedasticity
    group_data = [df[df[group_col] == group][dv_col].dropna() for group in groups]
    levene_stat, levene_p = stats.levene(*group_data)
    
    assumptions.append({
        'DV': dv_col,
        'test': 'levene',
        'statistic': levene_stat,
        'p_value': levene_p,
        'assumption': 'homoscedasticity',
        'status': 'pass' if levene_p > 0.05 else 'violation'
    })
    
    return assumptions

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 3: ANOVA Comparisons")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: Calibration groups with cognitive reserve indicators
        # Purpose: Compare groups on education, RPM, and age

        log("[LOAD] Loading calibration groups data...")
        df = pd.read_csv(RQ_DIR / "data" / "step02_calibration_groups.csv")
        log(f"[LOADED] step02_calibration_groups.csv ({len(df)} rows, {len(df.columns)} cols)")
        
        # Verify required columns
        required_cols = ['group', 'education', 'rpm', 'age']
        missing_cols = [col for col in required_cols if col not in df.columns]
        if missing_cols:
            raise ValueError(f"Missing required columns: {missing_cols}")
        
        # Check group sizes
        group_counts = df['group'].value_counts()
        log(f"[INFO] Group sizes: {dict(group_counts)}")
        for group, count in group_counts.items():
            if count < 15:
                log(f"[WARNING] Group {group} has only {count} participants (< 15 minimum)")

        # =========================================================================
        # STEP 2: Run ANOVAs for Each Dependent Variable
        # =========================================================================
        # Tool: one_way_anova_d068 (wrapped to handle signature mismatch)
        # What it does: Compare groups on education, RPM, age with dual p-values
        # Expected output: F-statistics, p-values (uncorrected + Bonferroni), effect sizes

        dependent_variables = ['education', 'rpm', 'age']
        n_comparisons = 6  # 3 ANOVAs + 3 correlations (from step04) = 6 total tests
        
        anova_results = []
        assumption_results = []
        posthoc_results = []

        for dv in dependent_variables:
            log(f"[ANALYSIS] Running ANOVA for {dv}...")
            
            # Check assumptions first
            log(f"[ASSUMPTIONS] Checking assumptions for {dv}...")
            assumptions = check_anova_assumptions(df, 'group', dv)
            assumption_results.extend(assumptions)
            
            # Run ANOVA with wrapper
            result = run_anova_d068_wrapper(df, 'group', dv, n_comparisons)
            
            # Calculate effect size (eta-squared)
            if 'f_statistic' in result and 'df_between' in result and 'df_within' in result:
                f_stat = result['f_statistic']
                df_between = result['df_between']
                df_within = result['df_within']
                eta_squared = (f_stat * df_between) / (f_stat * df_between + df_within)
            else:
                eta_squared = np.nan
            
            # Store ANOVA results
            anova_results.append({
                'DV': dv,
                'F_stat': result.get('f_statistic', np.nan),
                'p_uncorrected': result.get('p_uncorrected', np.nan),
                'p_bonferroni': result.get('p_corrected', np.nan),
                'eta_squared': eta_squared,
                'eta_ci_lower': np.nan,  # Would need bootstrap for CI
                'eta_ci_upper': np.nan
            })
            
            # Store post-hoc results if available
            if 'post_hoc_results' in result and result['post_hoc_results'] is not None:
                posthoc = result['post_hoc_results']
                if isinstance(posthoc, dict):
                    for comparison, comp_result in posthoc.items():
                        posthoc_results.append({
                            'DV': dv,
                            'comparison': comparison,
                            'mean_diff': comp_result.get('mean_diff', np.nan),
                            'se': comp_result.get('se', np.nan),
                            't_stat': comp_result.get('t_stat', np.nan),
                            'p_uncorrected': comp_result.get('p_uncorrected', np.nan),
                            'p_tukey': comp_result.get('p_tukey', np.nan),
                            'sig_uncorrected': comp_result.get('p_uncorrected', 1.0) < 0.05,
                            'sig_tukey': comp_result.get('p_tukey', 1.0) < 0.05
                        })
            
            # Log result safely
        f_val = result.get('f_statistic', 'N/A')
        p_val = result.get('p_uncorrected', 'N/A')
        if isinstance(f_val, (int, float)):
            log(f"[DONE] ANOVA for {dv}: F = {f_val:.3f}, p = {p_val:.3f}")
        else:
            log(f"[DONE] ANOVA for {dv}: F = {f_val}, p = {p_val}")

        # =========================================================================
        # STEP 3: Save Analysis Outputs
        # =========================================================================
        # These outputs will be used by: Step 05 (effect sizes) and final reporting

        log("[SAVE] Saving ANOVA results...")
        
        # Save main ANOVA results
        anova_df = pd.DataFrame(anova_results)
        anova_df.to_csv(RQ_DIR / "data" / "step03_anova_results.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step03_anova_results.csv ({len(anova_df)} rows, {len(anova_df.columns)} cols)")
        
        # Save assumption check results
        assumptions_df = pd.DataFrame(assumption_results)
        assumptions_df.to_csv(RQ_DIR / "data" / "step03_assumption_checks.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step03_assumption_checks.csv ({len(assumptions_df)} rows, {len(assumptions_df.columns)} cols)")
        
        # Save post-hoc results
        if posthoc_results:
            posthoc_df = pd.DataFrame(posthoc_results)
        else:
            # Create empty DataFrame with expected columns if no post-hoc results
            posthoc_df = pd.DataFrame(columns=['DV', 'comparison', 'mean_diff', 'se', 't_stat', 'p_uncorrected', 'p_tukey', 'sig_uncorrected', 'sig_tukey'])
        
        posthoc_df.to_csv(RQ_DIR / "data" / "step03_posthoc_comparisons.csv", index=False, encoding='utf-8')
        log(f"[SAVED] step03_posthoc_comparisons.csv ({len(posthoc_df)} rows, {len(posthoc_df.columns)} cols)")

        # =========================================================================
        # STEP 4: Run Validation Tool
        # =========================================================================
        # Tool: validate_hypothesis_test_dual_pvalues
        # Validates: Dual p-value reporting compliance (Decision D068)
        # Threshold: alpha_bonferroni = 0.05/6 = 0.0083

        log("[VALIDATION] Running validate_hypothesis_test_dual_pvalues...")
        
        # Prepare data for validation (add 'term' column as expected by validator)
        anova_df_for_validation = anova_df.copy()
        anova_df_for_validation['term'] = anova_df_for_validation['DV']  # Compatibility fix from gcode_lessons.md
        
        validation_result = validate_hypothesis_test_dual_pvalues(
            interaction_df=anova_df_for_validation,
            required_terms=['education', 'rpm', 'age'],
            alpha_bonferroni=0.0083  # 0.05/6 comparisons
        )

        # Report validation results
        # Expected: Confirms dual p-value reporting and required terms present
        if isinstance(validation_result, dict):
            for key, value in validation_result.items():
                log(f"[VALIDATION] {key}: {value}")
        else:
            log(f"[VALIDATION] {validation_result}")

        # Summary statistics
        log("[SUMMARY] ANOVA Results Summary:")
        for _, row in anova_df.iterrows():
            log(f"  {row['DV']}: F = {row['F_stat']:.3f}, p_uncorr = {row['p_uncorrected']:.3f}, p_bonf = {row['p_bonferroni']:.3f}")

        log("[SUCCESS] Step 3 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)