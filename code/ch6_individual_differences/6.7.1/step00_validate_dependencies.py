#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step00
Step Name: validate_dependencies
RQ: results/ch7/7.7.1
Generated: 2026-01-09

PURPOSE:
Verify Ch5 5.1.1 theta outputs and dfnonvr.csv accessibility before analysis

EXPECTED INPUTS:
  - results/ch5/5.1.1/status.yaml
    Format: YAML with rq_results status
    Expected: status='success'
  - results/ch5/5.1.1/data/step03_theta_scores.csv
    Columns: ['UID', 'test', 'Theta_All']
    Format: CSV with test-level theta scores (400 rows: 100 UIDs × 4 tests)
    Expected rows: ~400
  - data/dfnonvr.csv
    Columns: ['UID', 'ravlt-trial-1-score', ..., 'bvmt-trial-1-score', ...]
    Format: CSV with participant-level cognitive scores
    Expected rows: ~100

EXPECTED OUTPUTS:
  - results/ch7/7.7.1/data/step00_dependency_validation.txt
    Format: Text report with pass/fail status for each dependency
    Expected: All dependencies PASS

VALIDATION CRITERIA:
  - Ch5 5.1.1 status.yaml exists and indicates success
  - step03_theta_scores.csv exists with >=7000 bytes
  - dfnonvr.csv exists with >=50000 bytes
  - All required columns present

g_code REASONING:
- Approach: Validate each dependency file existence, size, and structure
- Why this approach: Prevents runtime failures in downstream steps
- Data flow: Check files → validate structure → report status
- Expected performance: <5 seconds

IMPLEMENTATION NOTES:
- Analysis tool: tools.validation.check_file_exists
- Validation tool: tools.validation.validate_data_columns
- Parameters: min_size_bytes=500 (detect empty files)
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import yaml
from typing import Dict, List

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/ch7/7.7.1/code/ (4 levels deep from project root)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import validation tools
from tools.validation import check_file_exists, validate_data_columns

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.7.1
LOG_FILE = RQ_DIR / "logs" / "step00_validate_dependencies.log"
OUTPUT_FILE = RQ_DIR / "data" / "step00_dependency_validation.txt"

# Dependencies to check
DEPENDENCIES = {
    'ch5_status': PROJECT_ROOT / 'results' / 'ch5' / '5.1.1' / 'status.yaml',
    'ch5_theta': PROJECT_ROOT / 'results' / 'ch5' / '5.1.1' / 'data' / 'step03_theta_scores.csv',
    'dfnonvr': PROJECT_ROOT / 'data' / 'dfnonvr.csv'
}

# Expected columns
EXPECTED_COLUMNS = {
    'ch5_theta': ['UID', 'test', 'Theta_All'],
    'dfnonvr': ['UID', 'ravlt-trial-1-score', 'ravlt-trial-2-score', 'ravlt-trial-3-score',
                'ravlt-trial-4-score', 'ravlt-trial-5-score', 'bvmt-trial-1-score',
                'bvmt-trial-2-score', 'bvmt-trial-3-score']
}

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()
    print(msg, flush=True)

# =============================================================================
# Main Validation
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 00: Validate Dependencies")

        # Create output directory if needed
        OUTPUT_FILE.parent.mkdir(parents=True, exist_ok=True)

        # Track validation results
        validation_results = []
        all_passed = True

        # =========================================================================
        # STEP 1: Check Ch5 5.1.1 status.yaml
        # =========================================================================
        # Expected: YAML file indicating Ch5 5.1.1 completed successfully
        # Purpose: Ensure upstream dependency is complete

        log("[CHECK] Validating Ch5 5.1.1 status.yaml...")
        status_path = DEPENDENCIES['ch5_status']

        if not status_path.exists():
            log(f"[FAIL] Ch5 status file not found: {status_path}")
            validation_results.append(f"FAIL: Ch5 5.1.1 status.yaml not found")
            all_passed = False
        else:
            # Load and check status
            with open(status_path, 'r') as f:
                status_data = yaml.safe_load(f)

            # Handle both dict and string formats (gcode_lessons.md #5)
            rq_results_status = status_data.get('rq_results', {})
            if isinstance(rq_results_status, dict):
                actual_status = rq_results_status.get('status')
            elif isinstance(rq_results_status, str):
                actual_status = rq_results_status
            else:
                actual_status = 'unknown'

            if actual_status == 'success':
                log(f"[PASS] Ch5 5.1.1 status: {actual_status}")
                validation_results.append(f"PASS: Ch5 5.1.1 status = success")
            else:
                log(f"[FAIL] Ch5 5.1.1 status: {actual_status} (expected 'success')")
                validation_results.append(f"FAIL: Ch5 5.1.1 status = {actual_status}")
                all_passed = False

        # =========================================================================
        # STEP 2: Check Ch5 theta scores file
        # =========================================================================
        # Expected: CSV with ~7KB size, 400 rows (100 UIDs × 4 tests)
        # Purpose: Validate theta scores available for aggregation

        log("[CHECK] Validating Ch5 theta scores CSV...")
        theta_path = DEPENDENCIES['ch5_theta']

        # Check file exists with minimum size (gcode_lessons.md pattern)
        file_check = check_file_exists(theta_path, min_size_bytes=500)

        if not file_check.get('valid', False):
            log(f"[FAIL] Ch5 theta file validation: {file_check.get('message', 'Unknown error')}")
            validation_results.append(f"FAIL: Ch5 theta file - {file_check.get('message')}")
            all_passed = False
        else:
            log(f"[PASS] Ch5 theta file exists ({file_check.get('size_bytes', 0)} bytes)")

            # Validate columns
            df_theta = pd.read_csv(theta_path)
            col_check = validate_data_columns(df_theta, EXPECTED_COLUMNS['ch5_theta'])

            if not col_check.get('valid', False):
                log(f"[FAIL] Ch5 theta columns: {col_check.get('missing_columns', [])}")
                validation_results.append(f"FAIL: Ch5 theta missing columns: {col_check.get('missing_columns')}")
                all_passed = False
            else:
                log(f"[PASS] Ch5 theta has required columns ({len(df_theta)} rows)")
                validation_results.append(f"PASS: Ch5 theta scores ({len(df_theta)} rows, {len(df_theta.columns)} cols)")

        # =========================================================================
        # STEP 3: Check dfnonvr.csv file
        # =========================================================================
        # Expected: CSV with cognitive test data, ~100 rows (participants)
        # Purpose: Validate RAVLT and BVMT scores available

        log("[CHECK] Validating dfnonvr.csv...")
        dfnonvr_path = DEPENDENCIES['dfnonvr']

        # Check file exists with minimum size
        file_check = check_file_exists(dfnonvr_path, min_size_bytes=10000)

        if not file_check.get('valid', False):
            log(f"[FAIL] dfnonvr.csv validation: {file_check.get('message', 'Unknown error')}")
            validation_results.append(f"FAIL: dfnonvr.csv - {file_check.get('message')}")
            all_passed = False
        else:
            log(f"[PASS] dfnonvr.csv exists ({file_check.get('size_bytes', 0)} bytes)")

            # Validate columns (critical cognitive test columns)
            df_nonvr = pd.read_csv(dfnonvr_path)
            col_check = validate_data_columns(df_nonvr, EXPECTED_COLUMNS['dfnonvr'])

            if not col_check.get('valid', False):
                log(f"[FAIL] dfnonvr.csv columns: {col_check.get('missing_columns', [])}")
                validation_results.append(f"FAIL: dfnonvr.csv missing columns: {col_check.get('missing_columns')}")
                all_passed = False
            else:
                log(f"[PASS] dfnonvr.csv has required columns ({len(df_nonvr)} rows)")
                validation_results.append(f"PASS: dfnonvr.csv ({len(df_nonvr)} rows, {len(df_nonvr.columns)} cols)")

        # =========================================================================
        # STEP 4: Write validation report
        # =========================================================================
        # Output: Text file summarizing all validation checks

        log("[SAVE] Writing validation report...")

        with open(OUTPUT_FILE, 'w', encoding='utf-8') as f:
            f.write("=" * 70 + "\n")
            f.write("RQ 7.7.1 DEPENDENCY VALIDATION REPORT\n")
            f.write("=" * 70 + "\n\n")

            f.write("OVERALL STATUS: ")
            if all_passed:
                f.write("ALL CHECKS PASSED\n\n")
            else:
                f.write("SOME CHECKS FAILED\n\n")

            f.write("INDIVIDUAL CHECKS:\n")
            f.write("-" * 70 + "\n")
            for result in validation_results:
                f.write(f"{result}\n")

            f.write("\n" + "=" * 70 + "\n")
            f.write("RECOMMENDATION:\n")
            if all_passed:
                f.write("All dependencies validated. Ready to proceed with Step 01.\n")
            else:
                f.write("Dependency validation failed. Address issues before proceeding.\n")

        log(f"[SAVED] Validation report: {OUTPUT_FILE}")

        # Final status
        if all_passed:
            log("[SUCCESS] Step 00 complete - All dependencies validated")
            sys.exit(0)
        else:
            log("[FAILED] Step 00 - Dependency validation failed")
            sys.exit(1)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        import traceback
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)
