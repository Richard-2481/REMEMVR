#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step00
Step Name: validate_dependencies
RQ: results/ch7/7.6.1
Generated: 2026-01-09

PURPOSE:
Verify Ch5 5.1.4 slope outputs and dfnonvr.csv cognitive data availability.
This step ensures all required dependencies from prior RQs are in place before
proceeding with the analysis. Critical for preventing downstream errors.

EXPECTED INPUTS:
None (this is a dependency validation step - no input files from pipeline)

EXPECTED OUTPUTS:
  - results/ch7/7.6.1/data/step00_dependency_validation.txt
    Format: Text report with validation results for each dependency
    Expected: Validation summary with PASS/FAIL status for each file

VALIDATION CRITERIA:
  - Ch5 5.1.4 status.yaml exists and contains "rq_results: success"
  - Ch5 5.1.4 slope file exists with minimum 1000 bytes
  - Slope file has columns: UID, random_slope
  - dfnonvr.csv exists with minimum 10000 bytes
  - dfnonvr.csv has cognitive test columns: UID, ravlt-trial-1-score, bvmt-trial-1-score, rpm-score

g_code REASONING:
- Approach: Validate file existence, size, and column structure for all dependencies
- Why this approach: Prevents wasted computation if dependencies are missing/incorrect
- Data flow: Check Ch5 5.1.4 outputs -> Check dfnonvr.csv -> Report validation status
- Expected performance: <5 seconds (only file checks, no computation)

IMPLEMENTATION NOTES:
- Analysis tool: check_file_exists from tools.validation (for file existence)
- Validation tool: Custom column validation (check actual CSV columns)
- Parameters: File paths, minimum sizes, expected columns
- Special handling: Parse status.yaml for nested 'rq_results' structure (dict or string)
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import yaml
from typing import Dict, List, Any

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = 7.6.1/
#   parents[2] = ch7/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import validation tool
from tools.validation import check_file_exists

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.6.1 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step00_validate_dependencies.log"
OUTPUT_FILE = RQ_DIR / "data" / "step00_dependency_validation.txt"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step05_lmm_model_comparison.csv
#   CORRECT: data/step00_dependency_validation.txt
#   WRONG:   results/lmm_model_comparison.csv  (wrong folder + no prefix)
#   WRONG:   data/dependency_validation.txt    (missing step prefix)

# =============================================================================
# Dependencies to Validate
# =============================================================================

DEPENDENCIES = {
    "ch5_status": {
        "path": PROJECT_ROOT / "results" / "ch5" / "5.1.4" / "status.yaml",
        "min_size": 100,
        "check_content": True,
        "required_content": "rq_results",
        "expected_status": "success",
        "description": "Ch5 5.1.4 completion status"
    },
    "ch5_slopes": {
        "path": PROJECT_ROOT / "results" / "ch5" / "5.1.4" / "data" / "step04_random_effects.csv",
        "min_size": 1000,
        "check_columns": True,
        "expected_columns": ["UID", "random_slope"],
        "description": "Individual slope estimates from Ch5 5.1.4"
    },
    "dfnonvr": {
        "path": PROJECT_ROOT / "data" / "dfnonvr.csv",
        "min_size": 10000,
        "check_columns": True,
        "expected_columns": ["UID", "ravlt-trial-1-score", "bvmt-trial-1-score", "rpm-score"],
        "description": "Cognitive test data"
    }
}

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()
    print(msg, flush=True)

# =============================================================================
# Validation Functions
# =============================================================================

def validate_yaml_content(yaml_path: Path, required_key: str, expected_status: str) -> Dict[str, Any]:
    """
    Validate that YAML file contains required key with expected status.
    Handles nested dict structure (rq_results.status) or flat string (rq_results: 'success').
    """
    try:
        with open(yaml_path, 'r', encoding='utf-8') as f:
            data = yaml.safe_load(f)

        if required_key not in data:
            return {
                'valid': False,
                'reason': f"Key '{required_key}' not found in YAML"
            }

        # Handle both dict and string formats (Lesson #5 from gcode_lessons.md)
        rq_results_value = data.get(required_key, {})
        if isinstance(rq_results_value, dict):
            actual_status = rq_results_value.get('status')
        elif isinstance(rq_results_value, str):
            actual_status = rq_results_value
        else:
            actual_status = 'unknown'

        if actual_status == expected_status:
            return {
                'valid': True,
                'status': actual_status
            }
        else:
            return {
                'valid': False,
                'reason': f"Expected status '{expected_status}', found '{actual_status}'"
            }

    except Exception as e:
        return {
            'valid': False,
            'reason': f"Error reading YAML: {str(e)}"
        }

def validate_csv_columns(csv_path: Path, expected_columns: List[str]) -> Dict[str, Any]:
    """
    Validate that CSV file has expected columns (exact match).
    """
    try:
        # Read only header (nrows=0 for speed)
        df = pd.read_csv(csv_path, nrows=0)
        actual_columns = df.columns.tolist()

        # Check if all expected columns are present
        missing_columns = [col for col in expected_columns if col not in actual_columns]

        if not missing_columns:
            return {
                'valid': True,
                'columns': actual_columns,
                'n_columns': len(actual_columns)
            }
        else:
            return {
                'valid': False,
                'reason': f"Missing columns: {missing_columns}",
                'expected': expected_columns,
                'actual': actual_columns
            }

    except Exception as e:
        return {
            'valid': False,
            'reason': f"Error reading CSV: {str(e)}"
        }

# =============================================================================
# Main Validation
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 00: Validate Dependencies")
        log("")

        # Initialize output report
        report_lines = []
        report_lines.append("=" * 80)
        report_lines.append("DEPENDENCY VALIDATION REPORT")
        report_lines.append("RQ 7.6.1: Cognitive Tests Predicting Individual Differences in Forgetting Rate")
        report_lines.append("Generated: 2026-01-09")
        report_lines.append("=" * 80)
        report_lines.append("")

        all_valid = True
        validation_results = {}

        # =========================================================================
        # STEP 1: Validate Each Dependency
        # =========================================================================

        for dep_name, dep_config in DEPENDENCIES.items():
            log(f"[VALIDATE] Checking {dep_name}: {dep_config['description']}")
            report_lines.append(f"Dependency: {dep_name}")
            report_lines.append(f"Description: {dep_config['description']}")
            report_lines.append(f"Path: {dep_config['path']}")

            # Check file existence and size
            file_check = check_file_exists(
                file_path=dep_config['path'],
                min_size_bytes=dep_config['min_size']
            )

            if not file_check.get('valid', False):
                all_valid = False
                log(f"[FAIL] {dep_name}: File check failed")
                report_lines.append(f"Status: [FAIL]")
                report_lines.append(f"Reason: {file_check.get('reason', 'File does not exist or too small')}")
            else:
                log(f"[PASS] {dep_name}: File exists ({file_check.get('size_bytes', 0)} bytes)")
                report_lines.append(f"Status: [PASS] File exists")
                report_lines.append(f"Size: {file_check.get('size_bytes', 0)} bytes")

                # Additional validation based on dependency type
                if dep_config.get('check_content', False):
                    # Validate YAML content
                    yaml_result = validate_yaml_content(
                        dep_config['path'],
                        dep_config['required_content'],
                        dep_config['expected_status']
                    )

                    if yaml_result.get('valid', False):
                        log(f"[PASS] {dep_name}: Status = {yaml_result.get('status')}")
                        report_lines.append(f"Content: [PASS] {dep_config['required_content']} = {yaml_result.get('status')}")
                    else:
                        all_valid = False
                        log(f"[FAIL] {dep_name}: {yaml_result.get('reason')}")
                        report_lines.append(f"Content: [FAIL] {yaml_result.get('reason')}")

                elif dep_config.get('check_columns', False):
                    # Validate CSV columns
                    csv_result = validate_csv_columns(
                        dep_config['path'],
                        dep_config['expected_columns']
                    )

                    if csv_result.get('valid', False):
                        log(f"[PASS] {dep_name}: Columns validated ({csv_result.get('n_columns')} total)")
                        report_lines.append(f"Columns: [PASS] Expected columns present")
                        report_lines.append(f"Expected: {', '.join(dep_config['expected_columns'])}")
                        report_lines.append(f"Total columns: {csv_result.get('n_columns')}")
                    else:
                        all_valid = False
                        log(f"[FAIL] {dep_name}: {csv_result.get('reason')}")
                        report_lines.append(f"Columns: [FAIL] {csv_result.get('reason')}")
                        report_lines.append(f"Expected: {', '.join(dep_config['expected_columns'])}")
                        report_lines.append(f"Actual: {', '.join(csv_result.get('actual', []))}")

            validation_results[dep_name] = file_check
            report_lines.append("")
            log("")

        # =========================================================================
        # STEP 2: Generate Summary Report
        # =========================================================================

        report_lines.append("=" * 80)
        report_lines.append("VALIDATION SUMMARY")
        report_lines.append("=" * 80)

        if all_valid:
            log("[SUCCESS] All dependencies validated successfully")
            report_lines.append("Overall Status: [PASS]")
            report_lines.append("")
            report_lines.append("All required dependencies are in place.")
            report_lines.append("Ready to proceed with RQ 7.6.1 analysis.")
        else:
            log("[FAIL] One or more dependencies failed validation")
            report_lines.append("Overall Status: [FAIL]")
            report_lines.append("")
            report_lines.append("One or more dependencies are missing or invalid.")
            report_lines.append("Please resolve issues before proceeding.")

        report_lines.append("")
        report_lines.append("=" * 80)
        report_lines.append("END OF REPORT")
        report_lines.append("=" * 80)

        # =========================================================================
        # STEP 3: Save Report to Output File
        # =========================================================================

        log(f"[SAVE] Writing validation report to {OUTPUT_FILE}")
        with open(OUTPUT_FILE, 'w', encoding='utf-8') as f:
            f.write('\n'.join(report_lines))
        log(f"[SAVED] Validation report written")

        # Exit with appropriate code
        if all_valid:
            log("[SUCCESS] Step 00 complete")
            sys.exit(0)
        else:
            log("[ERROR] Step 00 failed - dependencies not satisfied")
            sys.exit(1)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        import traceback
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)
