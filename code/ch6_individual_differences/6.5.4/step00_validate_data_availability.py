#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step00
Step Name: validate_data_availability
RQ: results/ch7/7.5.4
Generated: 2026-01-06

PURPOSE:
Validate dfvr.csv data availability and sleep variable completeness

EXPECTED INPUTS:
  - data/dfvr.csv
    Columns: ["UID", "TEST", "hours-slept-night-before", "sleep-quality--1=bad-1=good"]
    Format: CSV with 400 rows (100 participants × 4 tests)
    Expected rows: 400

EXPECTED OUTPUTS:
  - results/ch7/7.5.4/data/step00_data_validation.csv
    Columns: ["validation_result", "missing_columns", "row_count", "sleep_completeness"]
    Format: Validation results summary
    Expected rows: 1

VALIDATION CRITERIA:
  - expected_rows: 400
  - missing_data_threshold: 0.10

g_code REASONING:
- Approach: Load dfvr.csv and validate presence of required sleep columns
- Why this approach: Ensures downstream sleep analysis has required data
- Data flow: dfvr.csv → column validation → sleep data completeness check
- Expected performance: ~seconds

IMPLEMENTATION NOTES:
- Analysis tool: tools.validation.validate_data_columns
- Validation tool: tools.validation.validate_data_format
- Parameters: Required columns for sleep analysis per RQ 7.5.4 specification
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback

# Add project root to path for imports
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import validation tools
from tools.validation import validate_data_columns, validate_data_format

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.5.4
LOG_FILE = RQ_DIR / "logs" / "step00_validate_data_availability.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.log

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()  # Critical for real-time monitoring
    print(msg, flush=True)

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 00: validate_data_availability")

        # =========================================================================
        # STEP 1: Load dfvr.csv Data
        # =========================================================================
        # Expected: Complete VR test data with sleep variables
        # Purpose: Validate data availability for sleep-performance analysis

        log("[LOAD] Loading dfvr.csv data...")
        dfvr_path = PROJECT_ROOT / "data" / "dfvr.csv"
        
        if not dfvr_path.exists():
            log(f"[ERROR] dfvr.csv not found at {dfvr_path}")
            sys.exit(1)
            
        dfvr_df = pd.read_csv(dfvr_path)
        log(f"[LOADED] dfvr.csv ({len(dfvr_df)} rows, {len(dfvr_df.columns)} cols)")

        # =========================================================================
        # STEP 2: Run Data Column Validation
        # =========================================================================
        # Tool: validate_data_columns  
        # What it does: Check required columns exist in DataFrame
        # Expected output: Validation dict with column presence results

        log("[ANALYSIS] Running validate_data_columns...")
        required_columns = ["UID", "TEST", "hours-slept-night-before", "sleep-quality--1=bad-1=good"]
        
        validation_result = validate_data_columns(
            df=dfvr_df,
            required_columns=required_columns
        )
        log("[DONE] Column validation complete")

        # =========================================================================
        # STEP 3: Check Sleep Data Completeness  
        # =========================================================================
        # Additional validation: Check sleep variable completeness
        # Criteria: <10% missing data acceptable

        log("[VALIDATION] Checking sleep data completeness...")
        
        # Check missing data in sleep columns
        sleep_cols = ["hours-slept-night-before", "sleep-quality--1=bad-1=good"]
        missing_counts = {}
        
        for col in sleep_cols:
            if col in dfvr_df.columns:
                missing_count = dfvr_df[col].isna().sum()
                missing_pct = missing_count / len(dfvr_df)
                missing_counts[col] = {
                    'missing_count': missing_count,
                    'missing_pct': missing_pct,
                    'acceptable': missing_pct <= 0.10
                }
                log(f"[SLEEP] {col}: {missing_count}/{len(dfvr_df)} missing ({missing_pct:.1%})")
        
        # Overall sleep completeness
        all_sleep_acceptable = all(
            info['acceptable'] for info in missing_counts.values()
        )

        # =========================================================================
        # STEP 4: Save Validation Results
        # =========================================================================
        # Output: Validation summary for downstream steps

        log("[SAVE] Saving validation results...")
        
        # Create validation summary
        validation_summary = pd.DataFrame([{
            'validation_result': validation_result.get('valid', False),
            'missing_columns': str(validation_result.get('missing_cols', [])),
            'row_count': len(dfvr_df),
            'sleep_completeness': all_sleep_acceptable,
            'hours_slept_missing_pct': missing_counts.get('hours-slept-night-before', {}).get('missing_pct', 1.0),
            'sleep_quality_missing_pct': missing_counts.get('sleep-quality--1=bad-1=good', {}).get('missing_pct', 1.0),
            'expected_rows': 400,
            'row_count_acceptable': len(dfvr_df) >= 380  # Allow for some missing participants
        }])
        
        validation_path = RQ_DIR / "data" / "step00_data_validation.csv"
        validation_summary.to_csv(validation_path, index=False, encoding='utf-8')
        log(f"[SAVED] {validation_path} (1 row, {len(validation_summary.columns)} cols)")

        # =========================================================================
        # STEP 5: Run Format Validation
        # =========================================================================
        # Tool: validate_data_format
        # Validates: Basic DataFrame structure meets expectations
        
        log("[VALIDATION] Running validate_data_format...")
        
        format_validation = validate_data_format(
            df=dfvr_df, 
            required_cols=required_columns
        )

        # Report validation results
        if validation_result.get('valid', False):
            log("[VALIDATION] Column validation: PASS")
        else:
            log(f"[VALIDATION] Column validation: FAIL - {validation_result.get('message', 'Unknown error')}")
            
        if all_sleep_acceptable:
            log("[VALIDATION] Sleep completeness: PASS")
        else:
            log("[VALIDATION] Sleep completeness: FAIL - excessive missing data")
            
        if format_validation.get('valid', False):
            log("[VALIDATION] Format validation: PASS")
        else:
            log(f"[VALIDATION] Format validation: FAIL - {format_validation.get('message', 'Unknown error')}")

        # Scientific Mantra logging between steps
        log("")
        log("=== SCIENTIFIC MANTRA ===")
        log("1. What question did we ask?")
        log("   -> Are sleep variables available and complete in dfvr.csv for 400 observations?")
        log("2. What did we find?")
        log(f"   -> {len(dfvr_df)} total rows, sleep completeness: {all_sleep_acceptable}")
        log("3. What does it mean?")
        log("   -> Data validation determines if sleep-performance analysis can proceed")
        log("4. What should we do next?")
        log("   -> If validation passed: proceed to step01 sleep extraction")
        log("   -> If validation failed: investigate data quality issues")
        log("=========================")
        log("")

        log("[SUCCESS] Step 00 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)