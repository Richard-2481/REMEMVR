#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step01
Step Name: extract_paradigm_theta_scores
RQ: results/ch7/7.4.1
Generated: 2026-01-06 17:36:00

PURPOSE:
Load paradigm-specific theta scores from Ch5 5.3.1 results for RAVLT 
Free Recall vs Recognition process-specific prediction analysis.

EXPECTED INPUTS:
  - results/ch5/5.3.1/data/step03_theta_scores.csv
    Columns: ['composite_ID', 'domain_name', 'theta']
    Format: CSV with IRT theta scores from Ch5 paradigm analysis
    Expected rows: ~1200 (100 participants × 4 tests × 3 domains)

EXPECTED OUTPUTS:
  - results/ch7/7.4.1/data/step01_paradigm_theta.csv
    Columns: ['uid', 'theta_free_recall', 'theta_recognition']
    Format: CSV with aggregated paradigm-specific theta scores
    Expected rows: ~100 (one per participant)

VALIDATION CRITERIA:
  - theta_free_recall: Range [-3.0, 3.0] (IRT standard range)
  - 100 participants with theta scores for both paradigms
  - No missing values after aggregation
  - Both paradigms represented (Free Recall and Recognition)

g_code REASONING:
- Approach: Load Ch5 theta scores, filter to target paradigms, aggregate across domains/tests
- Why this approach: Need paradigm-specific ability estimates to test differential prediction
- Data flow: Ch5 long format → filter paradigms → aggregate by UID → wide format
- Expected performance: ~seconds (CSV processing, 1200 rows)

IMPLEMENTATION NOTES:
- Analysis tool: custom load_and_aggregate_paradigm_theta operation
- Validation tool: tools.validation.validate_numeric_range
- Parameters: source_path, paradigms ["IFR", "IRE"], domains ["What", "Where", "When"]
- Key transformations: composite_ID → UID extraction, domain aggregation, paradigm pivot
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = 7.4.1/
#   parents[2] = ch7/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import validation tool
from tools.validation import validate_numeric_range

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.4.1 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step01_extract_paradigm_theta.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step01_paradigm_theta.csv
#   CORRECT: logs/step01_extract_paradigm_theta.log
#   WRONG:   results/paradigm_theta.csv  (wrong folder + no prefix)
#   WRONG:   data/theta.csv              (missing step prefix)
#   WRONG:   logs/step01_theta.csv       (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
    print(msg)

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 1: extract_paradigm_theta_scores")

        # =========================================================================
        # STEP 1: Load Input Data from Ch5
        # =========================================================================
        # Expected: IRT theta scores from Ch5 5.3.1 paradigm analysis
        # Purpose: Extract paradigm-specific ability estimates for correlation analysis

        log("[LOAD] Loading Ch5 theta scores from Ch5 5.3.1...")
        source_path = PROJECT_ROOT / "results" / "ch5" / "5.3.1" / "data" / "step03_theta_scores.csv"
        
        if not source_path.exists():
            raise FileNotFoundError(f"Ch5 dependency missing: {source_path}")
            
        theta_df = pd.read_csv(source_path)
        log(f"[LOADED] Ch5 theta scores ({len(theta_df)} rows, {len(theta_df.columns)} cols)")
        
        # Validate expected columns
        expected_cols = ['composite_ID', 'domain_name', 'theta']
        if list(theta_df.columns) != expected_cols:
            raise ValueError(f"Unexpected columns. Expected {expected_cols}, got {list(theta_df.columns)}")
        
        log(f"[INFO] Unique domains: {sorted(theta_df['domain_name'].unique())}")

        # =========================================================================
        # STEP 2: Filter to Target Paradigms
        # =========================================================================
        # Target paradigms: Free Recall (free_recall) and Recognition (recognition)
        # Exclude: Cued Recall (cued_recall) - not needed for this analysis

        log("[FILTER] Filtering to free_recall and recognition paradigms...")
        target_paradigms = ['free_recall', 'recognition']
        
        # Filter to target paradigms
        paradigm_theta = theta_df[theta_df['domain_name'].isin(target_paradigms)].copy()
        log(f"[FILTERED] Kept {len(paradigm_theta)} rows with target paradigms")
        
        # Verify both paradigms are present
        actual_paradigms = set(paradigm_theta['domain_name'].unique())
        missing_paradigms = set(target_paradigms) - actual_paradigms
        if missing_paradigms:
            raise ValueError(f"Missing required paradigms: {missing_paradigms}")
        
        log(f"[VERIFIED] Both paradigms present: {sorted(actual_paradigms)}")

        # =========================================================================
        # STEP 3: Extract UID from composite_ID  
        # =========================================================================
        # Transform composite_ID format "A010_1" -> UID "A010"
        # Pattern: participant_test format

        log("[EXTRACT] Extracting UID from composite_ID...")
        
        # Extract UID (participant identifier) from composite_ID
        paradigm_theta['uid'] = paradigm_theta['composite_ID'].str.split('_').str[0]
        
        # Verify extraction worked
        sample_extraction = paradigm_theta[['composite_ID', 'uid']].head(3)
        log(f"[SAMPLE] UID extraction examples:")
        for _, row in sample_extraction.iterrows():
            log(f"  {row['composite_ID']} -> {row['uid']}")
        
        unique_uids = paradigm_theta['uid'].nunique()
        log(f"[INFO] Found {unique_uids} unique participants")

        # =========================================================================
        # STEP 4: Aggregate Theta Scores
        # =========================================================================
        # Aggregate theta scores per participant per paradigm
        # Mean across all domains (What, Where, When) and all 4 tests per paradigm

        log("[AGGREGATE] Computing mean theta per participant per paradigm...")
        
        # Group by UID and domain_name, compute mean theta
        # This aggregates across all test sessions (1-4) and implicitly across domains if multiple
        aggregated_theta = paradigm_theta.groupby(['uid', 'domain_name'])['theta'].mean().reset_index()
        
        log(f"[AGGREGATED] Reduced to {len(aggregated_theta)} participant-paradigm combinations")
        
        # Check data structure
        paradigm_counts = aggregated_theta.groupby('uid')['domain_name'].count()
        participants_with_both = (paradigm_counts == 2).sum()
        participants_with_one = (paradigm_counts == 1).sum()
        
        log(f"[INFO] Participants with both paradigms: {participants_with_both}")
        log(f"[INFO] Participants with only one paradigm: {participants_with_one}")
        
        if participants_with_one > 0:
            log("[WARNING] Some participants missing one paradigm - will be excluded from analysis")

        # =========================================================================
        # STEP 5: Reshape to Wide Format
        # =========================================================================
        # Transform to wide format: uid, theta_free_recall, theta_recognition
        # This creates the final analysis dataset

        log("[RESHAPE] Converting to wide format...")
        
        # Pivot to wide format
        wide_theta = aggregated_theta.pivot(index='uid', columns='domain_name', values='theta').reset_index()
        
        # Rename columns to match specification
        column_mapping = {
            'free_recall': 'theta_free_recall',
            'recognition': 'theta_recognition'
        }
        wide_theta = wide_theta.rename(columns=column_mapping)
        
        # Ensure all expected columns exist
        expected_output_cols = ['uid', 'theta_free_recall', 'theta_recognition']
        for col in expected_output_cols:
            if col not in wide_theta.columns:
                raise ValueError(f"Missing expected output column: {col}")
        
        # Select and order columns
        final_theta = wide_theta[expected_output_cols].copy()
        
        # Remove participants with missing paradigm data
        before_dropna = len(final_theta)
        final_theta = final_theta.dropna()
        after_dropna = len(final_theta)
        
        if before_dropna != after_dropna:
            log(f"[CLEANED] Removed {before_dropna - after_dropna} participants with missing paradigm data")
        
        log(f"[FINAL] Wide format dataset: {len(final_theta)} participants × {len(final_theta.columns)} columns")

        # =========================================================================
        # STEP 6: Save Analysis Output
        # =========================================================================
        # Save to data/ folder with step prefix as per folder conventions

        output_path = RQ_DIR / "data" / "step01_paradigm_theta.csv"
        log(f"[SAVE] Saving to {output_path}...")
        
        final_theta.to_csv(output_path, index=False, encoding='utf-8')
        log(f"[SAVED] step01_paradigm_theta.csv ({len(final_theta)} rows, {len(final_theta.columns)} cols)")
        
        # Log summary statistics
        log(f"[SUMMARY] Free recall theta: mean={final_theta['theta_free_recall'].mean():.3f}, std={final_theta['theta_free_recall'].std():.3f}")
        log(f"[SUMMARY] Recognition theta: mean={final_theta['theta_recognition'].mean():.3f}, std={final_theta['theta_recognition'].std():.3f}")

        # =========================================================================
        # STEP 7: Run Validation Tool
        # =========================================================================
        # Validate theta_free_recall values in IRT range [-3, 3]
        # Also implicitly validates recognition theta has similar range

        log("[VALIDATION] Running validate_numeric_range...")
        
        # Validate free recall theta range
        validation_result = validate_numeric_range(
            data=final_theta['theta_free_recall'],
            min_val=-3.0,
            max_val=3.0,
            column_name='theta_free_recall'
        )
        
        # Report validation results
        if isinstance(validation_result, dict):
            for key, value in validation_result.items():
                log(f"[VALIDATION] {key}: {value}")
        else:
            log(f"[VALIDATION] {validation_result}")
        
        # Additional validation checks
        log("[VALIDATION] Additional checks...")
        
        # Check expected number of participants (around 100)
        n_participants = len(final_theta)
        if n_participants < 50:
            log(f"[WARNING] Only {n_participants} participants - expected ~100")
        elif n_participants < 90:
            log(f"[INFO] {n_participants} participants (acceptable range)")
        else:
            log(f"[PASS] {n_participants} participants (expected range)")
        
        # Check no missing values
        missing_free_recall = final_theta['theta_free_recall'].isna().sum()
        missing_recognition = final_theta['theta_recognition'].isna().sum()
        
        if missing_free_recall == 0 and missing_recognition == 0:
            log("[PASS] No missing values in final dataset")
        else:
            log(f"[FAIL] Missing values: free_recall={missing_free_recall}, recognition={missing_recognition}")
        
        # Check both paradigms represented
        has_free_recall_data = not final_theta['theta_free_recall'].isna().all()
        has_recognition_data = not final_theta['theta_recognition'].isna().all()
        
        if has_free_recall_data and has_recognition_data:
            log("[PASS] Both paradigms represented in final dataset")
        else:
            log(f"[FAIL] Missing paradigm data: free_recall={has_free_recall_data}, recognition={has_recognition_data}")

        log("[SUCCESS] Step 1 complete")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)