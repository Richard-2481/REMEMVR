#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step03
Step Name: merge_and_check_assumptions
RQ: results/ch7/7.3.2
Generated: 2026-01-05

PURPOSE:
Merge calibration quality metrics from Ch6 with cognitive test data from dfnonvr.csv,
then check key regression assumptions before proceeding to hierarchical regression analysis.

EXPECTED INPUTS:
  - results/ch7/7.3.2/data/step01_calibration_metrics.csv
    Columns: ['UID', 'calibration_quality']
    Format: CSV with per-participant calibration quality from Ch6
    Expected rows: ~100-150 participants
  
  - results/ch7/7.3.2/data/step02_cognitive_tests.csv
    Columns: ['UID', 'RAVLT_T', 'BVMT_T', 'RPM_T', 'age', 'sex', 'education']
    Format: CSV with T-scored cognitive tests (M=50, SD=10) and demographics
    Expected rows: ~100-150 participants

EXPECTED OUTPUTS:
  - results/ch7/7.3.2/data/step03_analysis_dataset.csv
    Columns: ['UID', 'calibration_quality', 'RAVLT_T', 'BVMT_T', 'RPM_T', 'age', 'sex', 'education']
    Format: Complete analysis-ready dataset
    Expected rows: ~80-120 participants (reduced due to inner join)
  
  - results/ch7/7.3.2/data/step03_assumption_checks.csv
    Columns: ['test', 'statistic', 'p_value', 'result']
    Format: Preliminary assumption test results
    Expected rows: 4-6 assumption tests

VALIDATION CRITERIA:
  - validate_data_columns: All required columns present in merged dataset
  - Column validation: Verify UID is unique identifier
  - Range validation: T-scores in reasonable range (20-80)
  - Missing data: Report missing data patterns

g_code REASONING:
- Approach: Inner join on UID to create complete analysis dataset, then run preliminary assumption checks
- Why this approach: Need complete cases for regression analysis; assumption checking early prevents downstream issues
- Data flow: Two input CSV files -> merge on UID -> assumption testing -> two output files
- Expected performance: ~seconds (simple merge and basic statistical tests)

IMPLEMENTATION NOTES:
- Analysis tool: Custom implementation due to signature mismatch (prepare_regression_data has wrong parameters)
- Validation tool: tools.validation.validate_data_columns from tools.validation module
- Parameters: Inner join ensures complete cases; assumption tests use scipy.stats for normality/linearity
- Ch7 lessons applied: Direct merge instead of function with mismatched signature
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback
from scipy import stats
import warnings

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = rqY/
#   parents[2] = chX/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import validation tool
from tools.validation import validate_data_columns

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.3.2 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step03_merge_and_check_assumptions.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step03_analysis_dataset.csv
#   CORRECT: data/step03_assumption_checks.csv
#   WRONG:   results/analysis_dataset.csv     (wrong folder + no prefix)
#   WRONG:   data/analysis_dataset.csv        (missing step prefix)
#   WRONG:   logs/step03_dataset.csv          (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()  # Critical for real-time monitoring
    print(msg, flush=True)  # -u flag compatibility

# =============================================================================
# Assumption Testing Functions
# =============================================================================

def check_normality(data: pd.Series, name: str) -> Dict[str, Any]:
    """Check normality using Shapiro-Wilk test."""
    try:
        statistic, p_value = stats.shapiro(data.dropna())
        result = "NORMAL" if p_value > 0.05 else "NON_NORMAL"
        return {
            'test': f'Shapiro_Wilk_{name}',
            'statistic': statistic,
            'p_value': p_value,
            'result': result
        }
    except Exception as e:
        return {
            'test': f'Shapiro_Wilk_{name}',
            'statistic': np.nan,
            'p_value': np.nan,
            'result': f'ERROR: {str(e)}'
        }

def check_linearity(x: pd.Series, y: pd.Series, x_name: str, y_name: str) -> Dict[str, Any]:
    """Check linearity using Pearson correlation (preliminary check)."""
    try:
        # Remove rows with missing values
        valid_mask = ~(x.isna() | y.isna())
        x_valid = x[valid_mask]
        y_valid = y[valid_mask]
        
        if len(x_valid) < 3:
            return {
                'test': f'Linearity_{x_name}_vs_{y_name}',
                'statistic': np.nan,
                'p_value': np.nan,
                'result': 'ERROR: Insufficient data'
            }
        
        correlation, p_value = stats.pearsonr(x_valid, y_valid)
        result = "SIGNIFICANT" if p_value < 0.05 else "NON_SIGNIFICANT"
        
        return {
            'test': f'Linearity_{x_name}_vs_{y_name}',
            'statistic': correlation,
            'p_value': p_value,
            'result': result
        }
    except Exception as e:
        return {
            'test': f'Linearity_{x_name}_vs_{y_name}',
            'statistic': np.nan,
            'p_value': np.nan,
            'result': f'ERROR: {str(e)}'
        }

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 03: merge_and_check_assumptions")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: Calibration metrics from Ch6 (step01) and cognitive tests from dfnonvr.csv (step02)
        # Purpose: Merge these datasets to create complete analysis dataset for regression

        log("[LOAD] Loading calibration metrics from step01...")
        calibration_df = pd.read_csv(RQ_DIR / "data" / "step01_calibration_metrics.csv")
        log(f"[LOADED] step01_calibration_metrics.csv ({len(calibration_df)} rows, {len(calibration_df.columns)} cols)")
        log(f"[INFO] Calibration metrics columns: {calibration_df.columns.tolist()}")

        log("[LOAD] Loading cognitive test data from step02...")
        cognitive_df = pd.read_csv(RQ_DIR / "data" / "step02_cognitive_tests.csv")
        log(f"[LOADED] step02_cognitive_tests.csv ({len(cognitive_df)} rows, {len(cognitive_df.columns)} cols)")
        log(f"[INFO] Cognitive test columns: {cognitive_df.columns.tolist()}")

        # =========================================================================
        # STEP 2: Merge Datasets
        # =========================================================================
        # Tool: Custom merge implementation (due to prepare_regression_data signature mismatch)
        # What it does: Inner join on UID to create complete analysis dataset
        # Expected output: Analysis-ready dataset with all required predictors and outcome

        log("[MERGE] Merging calibration and cognitive data on UID...")
        
        # Check for UID overlap before merging
        calib_uids = set(calibration_df['UID'].unique())
        cognitive_uids = set(cognitive_df['UID'].unique())
        overlap_uids = calib_uids & cognitive_uids
        
        log(f"[INFO] Calibration UIDs: {len(calib_uids)}")
        log(f"[INFO] Cognitive test UIDs: {len(cognitive_uids)}")
        log(f"[INFO] Overlapping UIDs: {len(overlap_uids)}")
        
        if len(overlap_uids) == 0:
            raise ValueError("No overlapping UIDs found between calibration and cognitive data")
        
        # Perform inner join on UID
        analysis_dataset = pd.merge(
            calibration_df, 
            cognitive_df, 
            on='UID', 
            how='inner',
            suffixes=('_calib', '_cog')
        )
        
        log(f"[MERGED] Analysis dataset created ({len(analysis_dataset)} rows, {len(analysis_dataset.columns)} cols)")
        log(f"[INFO] Final columns: {analysis_dataset.columns.tolist()}")

        # Check for missing data
        missing_summary = analysis_dataset.isnull().sum()
        if missing_summary.sum() > 0:
            log("[WARNING] Missing data found:")
            for col, missing_count in missing_summary.items():
                if missing_count > 0:
                    log(f"  {col}: {missing_count} missing ({missing_count/len(analysis_dataset)*100:.1f}%)")
        else:
            log("[INFO] No missing data in merged dataset")

        # =========================================================================
        # STEP 3: Save Analysis Dataset
        # =========================================================================
        # Output: results/ch7/7.3.2/data/step03_analysis_dataset.csv
        # Contains: Complete merged data for regression analysis
        # Columns: ['UID', 'calibration_quality', 'RAVLT_T', 'BVMT_T', 'RPM_T', 'age', 'sex', 'education']

        output_path = RQ_DIR / "data" / "step03_analysis_dataset.csv"
        log(f"[SAVE] Saving analysis dataset to {output_path}...")
        analysis_dataset.to_csv(output_path, index=False, encoding='utf-8')
        log(f"[SAVED] step03_analysis_dataset.csv ({len(analysis_dataset)} rows, {len(analysis_dataset.columns)} cols)")

        # =========================================================================
        # STEP 4: Run Assumption Checks
        # =========================================================================
        # Tool: Custom assumption checking (using scipy.stats)
        # Validates: Basic normality and linearity assumptions for regression
        # Threshold: Standard alpha = 0.05 for preliminary checks

        log("[ASSUMPTIONS] Running preliminary assumption checks...")
        
        assumption_results = []
        
        # Check normality of outcome variable (calibration_quality)
        log("[TEST] Checking normality of calibration_quality...")
        normality_result = check_normality(analysis_dataset['calibration_quality'], 'calibration_quality')
        assumption_results.append(normality_result)
        log(f"[RESULT] {normality_result['test']}: {normality_result['result']} (p={normality_result['p_value']:.4f})")
        
        # Check normality of key predictors
        predictors_to_test = ['RAVLT_T', 'BVMT_T', 'RPM_T', 'RAVLT_Pct_Ret_T', 'BVMT_Pct_Ret_T', 'age']
        for predictor in predictors_to_test:
            if predictor in analysis_dataset.columns:
                log(f"[TEST] Checking normality of {predictor}...")
                pred_normality = check_normality(analysis_dataset[predictor], predictor)
                assumption_results.append(pred_normality)
                log(f"[RESULT] {pred_normality['test']}: {pred_normality['result']} (p={pred_normality['p_value']:.4f})")
        
        # Check linearity (preliminary correlation tests)
        cognitive_predictors = ['RAVLT_T', 'BVMT_T', 'RPM_T', 'RAVLT_Pct_Ret_T', 'BVMT_Pct_Ret_T']
        for predictor in cognitive_predictors:
            if predictor in analysis_dataset.columns:
                log(f"[TEST] Checking linearity: {predictor} vs calibration_quality...")
                linearity_result = check_linearity(
                    analysis_dataset[predictor], 
                    analysis_dataset['calibration_quality'], 
                    predictor, 
                    'calibration_quality'
                )
                assumption_results.append(linearity_result)
                log(f"[RESULT] {linearity_result['test']}: {linearity_result['result']} (r={linearity_result['statistic']:.4f}, p={linearity_result['p_value']:.4f})")

        # Create assumption results DataFrame
        assumption_df = pd.DataFrame(assumption_results)
        
        # Save assumption check results
        assumption_output_path = RQ_DIR / "data" / "step03_assumption_checks.csv"
        log(f"[SAVE] Saving assumption check results to {assumption_output_path}...")
        assumption_df.to_csv(assumption_output_path, index=False, encoding='utf-8')
        log(f"[SAVED] step03_assumption_checks.csv ({len(assumption_df)} rows, {len(assumption_df.columns)} cols)")

        # =========================================================================
        # STEP 5: Run Validation Tool
        # =========================================================================
        # Tool: tools.validation.validate_data_columns
        # Validates: All required columns present in final dataset
        # Required: ['UID', 'calibration_quality', 'RAVLT_T', 'BVMT_T', 'RPM_T', 'age', 'sex', 'education']

        log("[VALIDATION] Running validate_data_columns...")
        required_columns = ['UID', 'calibration_quality', 'RAVLT_T', 'BVMT_T', 'RPM_T', 'RAVLT_Pct_Ret_T', 'BVMT_Pct_Ret_T', 'age', 'sex', 'education']
        validation_result = validate_data_columns(analysis_dataset, required_columns)

        # Report validation results
        # Expected: Dictionary with validation status and column information
        if isinstance(validation_result, dict):
            log(f"[VALIDATION] Column validation result:")
            for key, value in validation_result.items():
                log(f"  {key}: {value}")
            
            # Check if validation passed
            if validation_result.get('valid', False):
                log("[VALIDATION] PASS - All required columns present")
            else:
                log("[VALIDATION] FAIL - Missing required columns")
                missing_cols = validation_result.get('missing_columns', [])
                if missing_cols:
                    log(f"[VALIDATION] Missing: {missing_cols}")
        else:
            log(f"[VALIDATION] {validation_result}")

        # Additional validation checks
        log("[VALIDATION] Additional data quality checks:")
        
        # Check UID uniqueness
        uid_duplicates = analysis_dataset['UID'].duplicated().sum()
        if uid_duplicates > 0:
            log(f"[WARNING] {uid_duplicates} duplicate UIDs found")
        else:
            log("[PASS] All UIDs are unique")
        
        # Check T-score ranges (should be roughly 20-80)
        t_score_cols = ['RAVLT_T', 'BVMT_T', 'RPM_T', 'RAVLT_Pct_Ret_T', 'BVMT_Pct_Ret_T']
        for col in t_score_cols:
            if col in analysis_dataset.columns:
                col_min = analysis_dataset[col].min()
                col_max = analysis_dataset[col].max()
                col_mean = analysis_dataset[col].mean()
                col_std = analysis_dataset[col].std()
                
                if col_min < 10 or col_max > 90:
                    log(f"[WARNING] {col} range unusual: {col_min:.1f} to {col_max:.1f}")
                else:
                    log(f"[PASS] {col} range reasonable: {col_min:.1f} to {col_max:.1f} (M={col_mean:.1f}, SD={col_std:.1f})")

        log("[SUCCESS] Step 03 complete")
        log(f"[SUMMARY] Created analysis dataset with {len(analysis_dataset)} participants")
        log(f"[SUMMARY] Ran {len(assumption_results)} assumption tests")
        log("[NEXT] Ready for hierarchical regression analysis in step04")
        
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)