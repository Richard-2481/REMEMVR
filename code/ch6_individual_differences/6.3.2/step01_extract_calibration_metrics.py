#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step01
Step Name: extract_calibration_metrics
RQ: results/ch7/7.3.2
Generated: 2026-01-05

PURPOSE:
Extract calibration quality metrics from Ch6 outputs - identifies per-participant
calibration quality by aggregating Ch6 per-test calibration scores to per-participant
level (mean across 4 tests per participant).

EXPECTED INPUTS:
  - results/ch6/6.2.1/data/step02_calibration_scores.csv
    Columns: ['UID', 'test', 'composite_ID', 'TSVR_hours', 'z_theta_accuracy', 'z_theta_confidence', 'calibration']
    Format: CSV with per-test calibration scores (4 tests per participant)
    Expected rows: ~400 (100 participants x 4 tests)

EXPECTED OUTPUTS:
  - results/ch7/7.3.2/data/step01_calibration_metrics.csv
    Columns: ['UID', 'calibration_quality']
    Format: Per-participant aggregated calibration quality
    Expected rows: ~100 (one per participant)

VALIDATION CRITERIA:
  - required_columns: ["UID", "calibration_quality"]

g_code REASONING:
- Approach: Load Ch6 calibration data, aggregate 'calibration' column by UID (mean across tests)
- Why this approach: Ch6 provides per-test calibration but Ch7 RQ 7.3.2 needs per-participant
  level for regression analysis with cognitive predictors
- Data flow: Ch6 per-test calibration → aggregate by UID → standardized output with
  'calibration_quality' column name for downstream regression
- Expected performance: ~5 minutes (simple aggregation operation)

IMPLEMENTATION NOTES:
- Analysis tool: Standard library pandas aggregation (no custom function needed)
- Validation tool: tools.validation.validate_data_columns 
- Parameters: Ch6 data source automatically discovered via glob pattern
- Key design: Uses 'calibration' column from Ch6, renames to 'calibration_quality' for consistency
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback
import glob
import os

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = rqY/ (7.3.2)
#   parents[2] = chX/ (ch7)
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import validation tool
from tools.validation import validate_data_columns

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/ch7/7.3.2 (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step01_extract_calibration_metrics.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step01_calibration_metrics.csv
#   CORRECT: logs/step01_extract_calibration_metrics.log
#   WRONG:   results/calibration_metrics.csv  (wrong folder + no prefix)
#   WRONG:   data/calibration_metrics.csv     (missing step prefix)
#   WRONG:   logs/step01_metrics.csv          (CSV in logs folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()  # Critical for real-time monitoring
    print(msg, flush=True)  # -u flag compatibility

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 01: Extract Calibration Metrics")

        # =========================================================================
        # STEP 1: Discover Ch6 Calibration Data Source
        # =========================================================================
        # Expected: Ch6 calibration analysis outputs with per-test calibration scores
        # Purpose: Find the source calibration data to aggregate by participant
        
        log("[DISCOVER] Searching for Ch6 calibration data...")
        
        # Specifically use step02_calibration_scores.csv which has per-test data we can aggregate
        calibration_file = str(PROJECT_ROOT / 'results' / 'ch6' / '6.2.1' / 'data' / 'step02_calibration_scores.csv')
        
        if not os.path.exists(calibration_file):
            # Fall back to searching
            ch6_patterns = [
                str(PROJECT_ROOT / 'results' / 'ch6' / '*' / 'data' / '*calibration*.csv'),
                str(PROJECT_ROOT / 'results' / 'ch6' / '*' / 'data' / '*resolution*.csv'),
                str(PROJECT_ROOT / 'results' / 'ch6' / '*' / 'data' / '*brier*.csv')
            ]
            
            calibration_file = None
            for pattern in ch6_patterns:
                files = glob.glob(pattern)
                # Skip trajectory files which don't have per-participant data
                files = [f for f in files if 'trajectory' not in f and 'SEM' not in f]
                if files:
                    calibration_file = files[0]
                    break
        
        if calibration_file and os.path.exists(calibration_file):
            log(f"[FOUND] Ch6 calibration source: {calibration_file}")
        else:
            calibration_file = None
        
        if not calibration_file:
            raise FileNotFoundError("No Ch6 calibration data found. Expected files matching patterns: " + str(ch6_patterns))

        # =========================================================================
        # STEP 2: Load Ch6 Calibration Data
        # =========================================================================
        # Expected: Per-test calibration scores (4 tests per participant)
        # Contains: UID, test, calibration (plus other columns)
        
        log("[LOAD] Loading Ch6 calibration data...")
        df_calibration = pd.read_csv(calibration_file)
        log(f"[LOADED] {calibration_file} ({len(df_calibration)} rows, {len(df_calibration.columns)} cols)")
        log(f"[COLUMNS] Available columns: {list(df_calibration.columns)}")
        
        # Verify required columns exist
        required_cols = ['UID', 'calibration']
        missing_cols = [col for col in required_cols if col not in df_calibration.columns]
        if missing_cols:
            # Check for alternative column names
            metric_cols = [col for col in df_calibration.columns if any(term in col.lower() 
                          for term in ['calibration', 'resolution', 'brier'])]
            if metric_cols:
                log(f"[INFO] Required 'calibration' column not found, but found calibration metrics: {metric_cols}")
                calibration_col = metric_cols[0]
                log(f"[INFO] Using '{calibration_col}' as calibration metric")
            else:
                raise ValueError(f"Missing required columns: {missing_cols}. Available: {list(df_calibration.columns)}")
        else:
            calibration_col = 'calibration'

        # =========================================================================
        # STEP 3: Aggregate Calibration by Participant
        # =========================================================================
        # Goal: Convert per-test calibration scores to per-participant calibration quality
        # Method: Mean calibration across 4 tests per participant
        
        log("[ANALYSIS] Aggregating calibration scores by participant...")
        
        # Check for missing data in calibration metric
        n_missing = df_calibration[calibration_col].isna().sum()
        if n_missing > 0:
            log(f"[WARNING] Found {n_missing} missing values in {calibration_col} column")
        
        # Aggregate by UID (mean across tests)
        df_calibration_agg = df_calibration.groupby('UID')[calibration_col].agg(['mean', 'count']).reset_index()
        df_calibration_agg.columns = ['UID', 'calibration_quality', 'n_tests']
        
        log(f"[AGGREGATED] {len(df_calibration_agg)} participants")
        log(f"[STATS] Mean calibration quality: {df_calibration_agg['calibration_quality'].mean():.3f}")
        log(f"[STATS] Tests per participant - Min: {df_calibration_agg['n_tests'].min()}, Max: {df_calibration_agg['n_tests'].max()}")
        
        # Check for participants with < 4 tests
        incomplete_participants = df_calibration_agg[df_calibration_agg['n_tests'] < 4]
        if len(incomplete_participants) > 0:
            log(f"[WARNING] {len(incomplete_participants)} participants have < 4 tests:")
            for _, row in incomplete_participants.iterrows():
                log(f"  {row['UID']}: {int(row['n_tests'])} tests")

        # =========================================================================
        # STEP 4: Save Analysis Output
        # =========================================================================
        # Output: Per-participant calibration quality for downstream regression analysis
        
        output_path = RQ_DIR / "data" / "step01_calibration_metrics.csv"
        log(f"[SAVE] Saving calibration metrics to {output_path}...")
        
        # Create final output with only required columns
        df_output = df_calibration_agg[['UID', 'calibration_quality']].copy()
        
        # Remove participants with missing calibration quality
        n_before = len(df_output)
        df_output = df_output.dropna()
        n_after = len(df_output)
        if n_before != n_after:
            log(f"[REMOVED] {n_before - n_after} participants with missing calibration quality")
        
        df_output.to_csv(output_path, index=False, encoding='utf-8')
        log(f"[SAVED] {output_path} ({len(df_output)} rows, {len(df_output.columns)} cols)")

        # =========================================================================
        # STEP 5: Run Validation Tool
        # =========================================================================
        # Tool: tools.validation.validate_data_columns
        # Validates: Output has required columns for downstream regression
        # Required columns: ["UID", "calibration_quality"]
        
        log("[VALIDATION] Running validate_data_columns...")
        validation_result = validate_data_columns(
            df=df_output,
            required_columns=["UID", "calibration_quality"]
        )

        # Report validation results
        # Expected: All required columns present, ready for regression analysis
        for key, value in validation_result.items():
            log(f"[VALIDATION] {key}: {value}")

        if not validation_result.get('valid', False):
            raise ValueError(f"Validation failed: {validation_result.get('message', 'Unknown validation error')}")

        log("[SUCCESS] Step 01 complete - calibration metrics extracted and validated")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)