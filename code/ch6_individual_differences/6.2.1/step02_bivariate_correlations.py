#!/usr/bin/env python3
# =============================================================================
# SCRIPT METADATA (Generated by g_code)
# =============================================================================
"""
Step ID: step02
Step Name: bivariate_correlations
RQ: results/ch7/7.2.1
Generated: 2026-01-04

PURPOSE:
Bivariate correlations with bootstrap CIs. Compute Pearson correlations between all 
cognitive variables (Age, theta_all, RAVLT_T, BVMT_T, RPM_T) with bootstrap confidence 
intervals, multiple comparison corrections, and dual p-value reporting per Decision D068.
Focus on Age-theta_all relationship per hypothesis.

EXPECTED INPUTS:
  - results/ch7/7.2.1/data/step01_analysis_dataset.csv
    Columns: ['UID', 'theta_all', 'Age', 'RAVLT_T', 'BVMT_T', 'RPM_T', 'Age_std', 'RAVLT_T_std', 'BVMT_T_std', 'RPM_T_std']
    Format: CSV with merged theta scores and cognitive test data
    Expected rows: ~100 (one per participant)

EXPECTED OUTPUTS:
  - results/ch7/7.2.1/data/step02_correlations.csv
    Columns: ['Variable1', 'Variable2', 'r', 'ci_lower', 'ci_upper', 'p_uncorrected', 'p_bonferroni', 'p_fdr']
    Format: CSV with correlation matrix, CIs, and dual p-values
    Expected rows: ~10-15 (unique variable pairs)

VALIDATION CRITERIA:
  - Dual p-value reporting: Each correlation must have p_uncorrected, p_bonferroni, p_fdr
  - Bootstrap CI coverage: 95% confidence intervals required
  - Multiple comparison correction: Both Bonferroni and FDR methods applied

g_code REASONING:
- Approach: Bootstrap confidence intervals for robust correlation estimates
- Why this approach: Accounts for sampling variability and provides robust CIs
- Data flow: Load merged data -> compute all pairwise correlations -> bootstrap CIs -> multiple comparison corrections -> save with dual p-values
- Expected performance: ~30 seconds (1000 bootstrap iterations on 100 participants)

IMPLEMENTATION NOTES:
- Analysis tool: tools.bootstrap.bootstrap_correlation_ci (adapted for parameter name differences)
- Validation tool: tools.validation.validate_hypothesis_test_dual_pvalues 
- Parameters: 1000 bootstrap iterations, 95% CI, Pearson correlations, seed=42 for reproducibility
- Signature adaptation: confidence_level->confidence, random_state->seed
"""
# =============================================================================

import sys
from pathlib import Path
import pandas as pd
import numpy as np
from typing import Dict, List, Tuple, Any
import traceback
from itertools import combinations
from scipy import stats

# Add project root to path for imports
# CRITICAL: RQ scripts are in results/chX/rqY/code/ (4 levels deep from project root)
# Path hierarchy from script location:
#   parents[0] = code/ (immediate parent)
#   parents[1] = rqY/
#   parents[2] = chX/
#   parents[3] = results/
#   parents[4] = REMEMVR/ (project root - THIS is what we need for imports)
PROJECT_ROOT = Path(__file__).resolve().parents[4]
sys.path.insert(0, str(PROJECT_ROOT))

# Import analysis tool
from tools.bootstrap import bootstrap_correlation_ci

# Import validation tool
from tools.validation import validate_hypothesis_test_dual_pvalues

# =============================================================================
# Configuration
# =============================================================================

RQ_DIR = Path(__file__).resolve().parents[1]  # results/chX/rqY (derived from script location)
LOG_FILE = RQ_DIR / "logs" / "step02_bivariate_correlations.log"

# =============================================================================
# FOLDER CONVENTIONS (MANDATORY - NO EXCEPTIONS)
# =============================================================================
#
# code/   : ONLY .py scripts (generated by g_code)
# data/   : ALL data outputs (.csv, .pkl, .txt) - ANY file produced by code
# logs/   : ONLY .log files - execution logs
# plots/  : ONLY image files (.png, .pdf, .svg) - actual plot images
# results/: ONLY final summary reports (.md, .html)
# docs/   : RQ documentation (concept, plan, analysis specs)
#
# NAMING CONVENTION (MANDATORY):
# ALL files in data/ and logs/ MUST be prefixed with step number:
#   - stepXX_descriptive_name.csv
#   - stepXX_descriptive_name.pkl
#   - stepXX_descriptive_name.log
#
# Examples:
#   CORRECT: data/step02_correlations.csv
#   CORRECT: logs/step02_bivariate_correlations.log
#   WRONG:   results/correlations.csv  (wrong folder + no prefix)
#   WRONG:   data/correlations.csv     (missing step prefix)
#   WRONG:   plots/step02_correlations.csv  (CSV in plots folder)

# =============================================================================
# Logging Function
# =============================================================================

def log(msg):
    """Write to both log file and console."""
    with open(LOG_FILE, 'a', encoding='utf-8') as f:
        f.write(f"{msg}\n")
        f.flush()  # Critical for real-time monitoring
    print(msg, flush=True)  # -u flag compatibility

# =============================================================================
# Main Analysis
# =============================================================================

if __name__ == "__main__":
    try:
        log("[START] Step 2: Bivariate Correlations with Bootstrap CIs")

        # =========================================================================
        # STEP 1: Load Input Data
        # =========================================================================
        # Expected: Merged dataset from Step 1 with theta scores + cognitive tests
        # Purpose: Compute all pairwise correlations between cognitive variables

        log("[LOAD] Loading merged analysis dataset...")
        # Load step01_analysis_dataset.csv
        # Expected columns: Age, theta_all, RAVLT_T, BVMT_T, RPM_T (plus standardized versions)
        # Expected rows: ~100 (one per participant)
        input_df = pd.read_csv(RQ_DIR / "data" / "step01_analysis_dataset.csv")
        log(f"[LOADED] step01_analysis_dataset.csv ({len(input_df)} rows, {len(input_df.columns)} cols)")
        
        # Extract variables for correlation analysis (use raw scores, not standardized)
        correlation_variables = ['Age', 'theta_all', 'RAVLT_T', 'BVMT_T', 'RPM_T', 'RAVLT_Pct_Ret_T', 'BVMT_Pct_Ret_T']
        
        # Verify all required variables present
        missing_vars = [var for var in correlation_variables if var not in input_df.columns]
        if missing_vars:
            raise ValueError(f"Missing variables: {missing_vars}")
        
        log(f"[INFO] Correlation variables: {correlation_variables}")
        
        # Extract correlation data
        corr_data = input_df[correlation_variables].copy()
        
        # Check for missing data
        missing_count = corr_data.isnull().sum().sum()
        if missing_count > 0:
            log(f"[WARNING] Missing data detected: {missing_count} values")
            # Drop rows with any missing values
            corr_data = corr_data.dropna()
            log(f"[INFO] After dropping missing data: {len(corr_data)} rows")
        
        log(f"[INFO] Final correlation data: {len(corr_data)} participants, {len(correlation_variables)} variables")

        # =========================================================================
        # STEP 2: Run Bivariate Correlations with Bootstrap CIs
        # =========================================================================
        # Tool: bootstrap_correlation_ci (adapted for actual signature)
        # What it does: Computes correlation + bootstrap confidence intervals
        # Expected output: All pairwise correlations with robust CIs

        log("[ANALYSIS] Computing pairwise correlations with bootstrap CIs...")
        
        # Parameters for bootstrap analysis
        n_bootstrap = 1000
        confidence_level = 0.95
        method = "pearson"
        random_seed = 42
        
        correlation_results = []
        
        # Compute all pairwise correlations
        for var1, var2 in combinations(correlation_variables, 2):
            log(f"[ANALYSIS] Computing correlation: {var1} vs {var2}")
            
            x = corr_data[var1].values
            y = corr_data[var2].values
            
            # Use bootstrap_correlation_ci with adapted parameters
            # Note: actual signature uses 'confidence' not 'confidence_level', 'seed' not 'random_state'
            bootstrap_result = bootstrap_correlation_ci(
                x=x,
                y=y,
                n_bootstrap=n_bootstrap,
                confidence=confidence_level,  # Adapted parameter name
                method=method,
                seed=random_seed  # Adapted parameter name
            )
            
            # Compute uncorrected p-value using scipy
            pearson_r, pearson_p = stats.pearsonr(x, y)
            
            # Check what keys are in bootstrap_result
            if isinstance(bootstrap_result, dict):
                # Try different possible key names
                if 'r' in bootstrap_result:
                    corr_value = bootstrap_result['r']
                elif 'correlation' in bootstrap_result:
                    corr_value = bootstrap_result['correlation']
                elif 'estimate' in bootstrap_result:
                    corr_value = bootstrap_result['estimate']
                else:
                    # Use Pearson r as fallback
                    corr_value = pearson_r
                    log(f"[WARN] Using pearson_r, bootstrap keys: {list(bootstrap_result.keys())}")
            else:
                # If it's not a dict, assume it's the correlation value
                corr_value = bootstrap_result
                bootstrap_result = {'ci_lower': None, 'ci_upper': None}
            
            correlation_results.append({
                'Variable1': var1,
                'Variable2': var2,
                'r': corr_value,
                'ci_lower': bootstrap_result.get('ci_lower', bootstrap_result.get('ci_low', None)),
                'ci_upper': bootstrap_result.get('ci_upper', bootstrap_result.get('ci_high', None)),
                'p_uncorrected': pearson_p,
                'n_observations': len(x)
            })
            
            ci_lower = bootstrap_result.get('ci_lower', bootstrap_result.get('ci_low', 'N/A'))
            ci_upper = bootstrap_result.get('ci_upper', bootstrap_result.get('ci_high', 'N/A'))
            log(f"[RESULT] {var1} vs {var2}: r = {corr_value:.3f}, 95% CI [{ci_lower:.3f}, {ci_upper:.3f}], p = {pearson_p:.3f}")
        
        log("[DONE] Bootstrap correlation analysis complete")

        # =========================================================================
        # STEP 3: Multiple Comparison Corrections (Decision D068)
        # =========================================================================
        # Apply both Bonferroni and FDR corrections for dual p-value reporting
        # Required per Decision D068 for all hypothesis tests

        log("[ANALYSIS] Applying multiple comparison corrections...")
        
        from statsmodels.stats.multitest import multipletests
        
        # Extract p-values for correction
        p_values = [result['p_uncorrected'] for result in correlation_results]
        
        # Bonferroni correction
        # For bivariate correlations: 10 pairwise tests (5 choose 2)
        alpha_bonferroni = 0.05 / len(p_values)
        p_bonferroni = [p * len(p_values) for p in p_values]  # Manual Bonferroni
        
        # FDR correction using Benjamini-Hochberg
        reject_fdr, p_fdr, alpha_sidak, alpha_bonf = multipletests(p_values, method='fdr_bh')
        
        # Add corrected p-values to results
        for i, result in enumerate(correlation_results):
            result['p_bonferroni'] = min(p_bonferroni[i], 1.0)  # Cap at 1.0
            result['p_fdr'] = p_fdr[i]
            result['significant_bonferroni'] = p_bonferroni[i] < 0.05
            result['significant_fdr'] = reject_fdr[i]
        
        log(f"[DONE] Multiple comparison corrections applied (Bonferroni alpha = {alpha_bonferroni:.4f})")
        
        # Report key findings
        age_theta_result = next((r for r in correlation_results if 
                               set([r['Variable1'], r['Variable2']]) == {'Age', 'theta_all'}), None)
        
        if age_theta_result:
            log(f"[KEY FINDING] Age-theta_all correlation: r = {age_theta_result['r']:.3f}, "
                f"95% CI [{age_theta_result['ci_lower']:.3f}, {age_theta_result['ci_upper']:.3f}], "
                f"p_uncorrected = {age_theta_result['p_uncorrected']:.3f}, "
                f"p_bonferroni = {age_theta_result['p_bonferroni']:.3f}, "
                f"p_fdr = {age_theta_result['p_fdr']:.3f}")
            
            if age_theta_result['r'] < -0.15:
                log("[HYPOTHESIS] Age-theta_all correlation meets expected small negative threshold (< -0.15)")
            else:
                log(f"[HYPOTHESIS] Age-theta_all correlation ({age_theta_result['r']:.3f}) does not meet expected threshold (< -0.15)")

        # =========================================================================
        # STEP 4: Save Analysis Outputs
        # =========================================================================
        # These outputs will be used by: Step 3 hierarchical regression, final summary

        log("[SAVE] Saving correlation results with dual p-values...")
        
        # Create DataFrame with required columns for D068 compliance
        results_df = pd.DataFrame(correlation_results)
        
        # Reorder columns to match specification
        output_columns = ['Variable1', 'Variable2', 'r', 'ci_lower', 'ci_upper', 
                         'p_uncorrected', 'p_bonferroni', 'p_fdr']
        results_df = results_df[output_columns]
        
        # Output: step02_correlations.csv
        # Contains: Correlation matrix with bootstrap CIs and dual p-values
        # Columns: Variable1, Variable2, r, ci_lower, ci_upper, p_uncorrected, p_bonferroni, p_fdr
        output_path = RQ_DIR / "data" / "step02_correlations.csv"
        results_df.to_csv(output_path, index=False, encoding='utf-8')
        log(f"[SAVED] step02_correlations.csv ({len(results_df)} rows, {len(results_df.columns)} cols)")
        
        # Summary statistics
        significant_bonferroni = (results_df['p_bonferroni'] < 0.05).sum()
        significant_fdr = (results_df['p_fdr'] < 0.05).sum()
        log(f"[SUMMARY] {len(results_df)} correlations computed")
        log(f"[SUMMARY] Significant after Bonferroni correction: {significant_bonferroni}/{len(results_df)}")
        log(f"[SUMMARY] Significant after FDR correction: {significant_fdr}/{len(results_df)}")

        # =========================================================================
        # STEP 5: Run Validation Tool
        # =========================================================================
        # Tool: validate_hypothesis_test_dual_pvalues
        # Validates: Dual p-value reporting compliance (D068)
        # Threshold: Checks for required columns and format

        log("[VALIDATION] Running validate_hypothesis_test_dual_pvalues...")
        
        # Prepare data for validation (function expects 'term' column)
        validation_df = results_df.copy()
        validation_df['term'] = validation_df['Variable1'] + ':' + validation_df['Variable2']
        
        # Required terms for validation (all correlation pairs)
        required_terms = validation_df['term'].tolist()
        
        validation_result = validate_hypothesis_test_dual_pvalues(
            interaction_df=validation_df,
            required_terms=required_terms,
            alpha_bonferroni=0.05  # Standard alpha level
        )

        # Report validation results
        # Expected: Validation of dual p-value reporting format
        if isinstance(validation_result, dict):
            for key, value in validation_result.items():
                log(f"[VALIDATION] {key}: {value}")
        else:
            log(f"[VALIDATION] {validation_result}")
        
        # Check validation passed
        if isinstance(validation_result, dict) and validation_result.get('valid', False):
            log("[VALIDATION] PASSED - Dual p-value reporting format verified")
        else:
            log("[VALIDATION] WARNING - Validation concerns detected (may still be acceptable)")

        log("[SUCCESS] Step 2 complete - Bivariate correlations with bootstrap CIs and dual p-values")
        sys.exit(0)

    except Exception as e:
        log(f"[ERROR] {str(e)}")
        log("[TRACEBACK] Full error details:")
        with open(LOG_FILE, 'a', encoding='utf-8') as f:
            traceback.print_exc(file=f)
        traceback.print_exc()
        sys.exit(1)